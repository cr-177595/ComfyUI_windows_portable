# LTXVImgToVideoInplace

### نمای کلی

گره LTXVImgToVideoInplace با رمزگذاری یک تصویر ورودی در فریم‌های ابتدایی آن، یک نمایش نهفته ویدیویی را شرطی می‌کند. عملکرد آن به این صورت است که با استفاده از یک VAE، تصویر را به فضای نهفته رمزگذاری کرده و سپس بر اساس شدت مشخصی، آن را با نمونه‌های نهفته موجود ترکیب می‌کند. این امکان را فراهم می‌سازد که یک تصویر به عنوان نقطه شروع یا سیگنال شرطی‌سازی برای تولید ویدیو عمل کند.

### ورودی‌ها

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
| --- | --- | --- | --- | --- |
| `vae` | مدل VAE مورد استفاده برای رمزگذاری تصویر ورودی به فضای نهفته. | VAE | بله | - |
| `image` | تصویر ورودی که باید رمزگذاری شده و برای شرطی‌سازی نهفته ویدیویی استفاده شود. | IMAGE | بله | - |
| `latent` | نمایش نهفته ویدیویی هدف که باید تغییر یابد. | LATENT | بله | - |
| `strength` | شدت ترکیب تصویر رمزگذاری‌شده در نهفته را کنترل می‌کند. مقدار 1.0 فریم‌های ابتدایی را کاملاً جایگزین می‌کند، در حالی که مقادیر پایین‌تر آن‌ها را ترکیب می‌کند. (پیش‌فرض: 1.0) | FLOAT | خیر | 0.0 - 1.0 |
| `bypass` | شرطی‌سازی را دور می‌زند. در صورت فعال بودن، گره نهفته ورودی را بدون تغییر برمی‌گرداند. (پیش‌فرض: False) | BOOLEAN | خیر | - |

**نکته:** تصویر `image` به طور خودکار تغییر اندازه می‌دهد تا با ابعاد فضایی مورد نیاز `vae` برای رمزگذاری، بر اساس عرض و ارتفاع ورودی `latent`، مطابقت داشته باشد.

### خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
| --- | --- | --- |
| `latent` | نمایش نهفته ویدیویی تغییر یافته. شامل نمونه‌های به‌روزرسانی‌شده و یک `noise_mask` است که شدت شرطی‌سازی را روی فریم‌های ابتدایی اعمال می‌کند. | LATENT |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LTXVImgToVideoInplace/fa.md)

---
**Source fingerprint (SHA-256):** `49df511591071f51e2b86f2302cfb438d18b5e1ade7ef228345f65fddf88dbcc`
