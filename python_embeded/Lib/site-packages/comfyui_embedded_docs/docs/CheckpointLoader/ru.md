# Загрузить сheckpoint с конфигурацией (УСТАРЕЛО)

Узел CheckpointLoader загружает предварительно обученную контрольную точку модели вместе с её конфигурационным файлом. Он принимает конфигурационный файл и файл контрольной точки в качестве входных данных и возвращает загруженные компоненты модели, включая основную модель, модель CLIP и модель VAE для использования в рабочем процессе.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `название_конфигурации` | Конфигурационный файл, определяющий архитектуру и настройки модели | STRING | Да | Доступные конфигурационные файлы |
| `название_точки_проверки` | Файл контрольной точки, содержащий обученные веса и параметры модели | STRING | Да | Доступные файлы контрольных точек |

**Примечание:** Для этого узла требуется выбрать как конфигурационный файл, так и файл контрольной точки. Конфигурационный файл должен соответствовать архитектуре загружаемого файла контрольной точки.

## Выходные параметры

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `MODEL` | Загруженный основной компонент модели, готовый к инференсу | MODEL |
| `CLIP` | Загруженный компонент модели CLIP для кодирования текста | CLIP |
| `VAE` | Загруженный компонент модели VAE для кодирования и декодирования изображений | VAE |

**Важное примечание:** Этот узел помечен как устаревший и может быть удалён в будущих версиях. Для новых рабочих процессов рекомендуется использовать альтернативные узлы загрузки.

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/CheckpointLoader/ru.md)

---
**Source fingerprint (SHA-256):** `9977bda5e124a9d10566839cbee868c74fab120c454141f27ce145efa60105e9`
