# محمل الشبكات الفائقة

سيكتشف هذا العقد النماذج الموجودة في مجلد `ComfyUI/models/hypernetworks`، كما سيقرأ النماذج من المسارات الإضافية المُهيأة في ملف extra_model_paths.yaml. قد تحتاج أحيانًا إلى **تحديث واجهة ComfyUI** للسماح لها بقراءة ملفات النموذج من المجلد المناسب.

تم تصميم عقدة HypernetworkLoader لتعزيز أو تعديل قدرات نموذج معين من خلال تطبيق شبكة فائقة (hypernetwork). تقوم هذه العقدة بتحميل شبكة فائقة محددة وتطبيقها على النموذج، مما قد يُغير سلوكه أو أداءه بناءً على معامل القوة (strength). تتيح هذه العملية إجراء تعديلات ديناميكية على بنية النموذج أو معاملاته، مما يُمكن أنظمة الذكاء الاصطناعي من أن تكون أكثر مرونة وقابلية للتكيف.

## المدخلات

| الحقل | الوصف | نوع Comfy |
| --- | --- | --- |
| `النموذج` | النموذج الأساسي الذي سيتم تطبيق الشبكة الفائقة عليه، ويحدد البنية التي سيتم تعزيزها أو تعديلها. | `MODEL` |
| `اسم_الشبكة_الفائقة` | اسم الشبكة الفائقة التي سيتم تحميلها وتطبيقها على النموذج، مما يؤثر على السلوك المُعدّل أو أداء النموذج. | `COMBO[STRING]` |
| `القوة` | قيمة قياسية تُضبط شدة تأثير الشبكة الفائقة على النموذج، مما يسمح بضبط دقيق للتعديلات. | `FLOAT` |

## المخرجات

| الحقل | الوصف | نوع البيانات |
| --- | --- | --- |
| `النموذج` | النموذج المُعدّل بعد تطبيق الشبكة الفائقة، مما يُظهر تأثير الشبكة الفائقة على النموذج الأصلي. | `MODEL` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/HypernetworkLoader/ar.md)
