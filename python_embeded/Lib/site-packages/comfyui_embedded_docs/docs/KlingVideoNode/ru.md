# Kling 3.0 Видео

Вот перевод документации на русский язык:

Этот узел генерирует видео с использованием модели Kling V3. Он поддерживает два основных режима: текст-в-видео, где видео создаётся из текстового описания, и изображение-в-видео, где анимируется существующее изображение. Также предлагаются расширенные функции, такие как создание мультисегментных видео с разными подсказками для каждой части (раскадровки) и опциональная генерация сопутствующего аудио.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `мульти-шот` | Управляет созданием одного видео или серии сегментов с индивидуальными подсказками и длительностью. Если значение не "disabled", появляются дополнительные входы для подсказки и длительности каждой раскадровки. | COMBO | Да | `"disabled"`<br>`"1 storyboard"`<br>`"2 storyboards"`<br>`"3 storyboards"`<br>`"4 storyboards"`<br>`"5 storyboards"`<br>`"6 storyboards"` |
| `генерировать аудио` | При включении узел генерирует аудио для видео. По умолчанию `True`. | BOOLEAN | Да | `True` / `False` |
| `модель` | Модель и связанные с ней настройки. Выбор этой опции открывает подпараметры `resolution` и `aspect_ratio`. | COMBO | Да | `"kling-v3"` |
| `model.resolution` | Разрешение генерируемого видео. Эта настройка доступна, когда `модель` установлена на "kling-v3". | COMBO | Да | `"4k"`<br>`"1080p"`<br>`"720p"` |
| `model.aspect_ratio` | Соотношение сторон генерируемого видео. Эта настройка игнорируется, если для `стартовый кадр` указано изображение (режим изображение-в-видео). Доступна, когда `модель` установлена на "kling-v3". | COMBO | Да | `"16:9"`<br>`"9:16"`<br>`"1:1"` |
| `seed` | Значение зерна для генерации. Изменение этого значения приведёт к повторному запуску узла, но результаты не являются детерминированными. По умолчанию `0`. | INT | Да | от 0 до 2147483647 |
| `стартовый кадр` | Необязательное начальное изображение. При подключении узел переключается из режима текст-в-видео в режим изображение-в-видео, анимируя предоставленное изображение. | IMAGE | Нет | - |

**Входные параметры для режима `multi_shot`:**

* Когда `multi_shot` установлен в **"disabled"**, появляются следующие входы:
  * `prompt` (STRING): Основное текстовое описание видео. Обязательно. Должно содержать от 1 до 2500 символов.
  * `negative_prompt` (STRING): Текст, описывающий то, что не должно появляться в видео. Необязательно.
  * `duration` (INT): Длительность видео в секундах. Должна быть от 3 до 15. По умолчанию `5`.
* Когда `multi_shot` установлен в один из вариантов раскадровки (например, `"3 storyboards"`), появляются входы для каждого сегмента раскадровки (например, `storyboard_1_prompt`, `storyboard_1_duration`). Каждая подсказка должна содержать от 1 до 512 символов. **Общая сумма длительностей всех раскадровок** должна быть от 3 до 15 секунд.

**Ограничения:**

* Узел работает в режиме **текст-в-видео**, когда `start_frame` не подключён. В этом режиме используется настройка `model.aspect_ratio`.
* Узел работает в режиме **изображение-в-видео**, когда `start_frame` подключён. Настройка `model.aspect_ratio` игнорируется. Входное изображение должно быть не менее 300x300 пикселей и иметь соотношение сторон от 1:2.5 до 2.5:1.
* В режиме раскадровки (`multi_shot` не "disabled") основные входы `prompt` и `negative_prompt` скрыты и не используются.

## Выходные параметры

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `video` | Сгенерированный видеофайл. | VIDEO |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/KlingVideoNode/ru.md)

---
**Source fingerprint (SHA-256):** `f7f827d657b1d057d273eba3215ce6848d3ea05c5f348e2f3fccccfdd030dfc3`
