# FluxProImageNode

أنت خبير في الترجمة التقنية متخصص في توثيق عُقد ComfyUI من الإنجليزية إلى العربية.

## قواعد الترجمة

1. **المحتوى الذي يجب عدم ترجمته:**
   - أسماء المعاملات بين علامات الاقتباس الخلفية: `image`, `seed`, `model`
   - أنواع البيانات بالأحرف الكبيرة: IMAGE, STRING, INT, FLOAT, MODEL, CONDITIONING, إلخ
   - القيم في عمود Range: الأرقام، "auto"، أسماء الخيارات
   - الكود، مسارات الملفات

2. **المحتوى الذي يجب ترجمته:**
   - عناوين الأقسام: ## نظرة عامة, ## المدخلات, ## المخرجات
   - جميع النصوص الوصفية والتوضيحية
   - أوصاف المعاملات

3. **جودة الترجمة:**
   - استخدام اللغة العربية الفصحى المعاصرة
   - الحفاظ على نبرة احترافية ولكن سهلة الفهم
   - ضمان الدقة التقنية
   - استخدام المصطلحات التقنية العربية القياسية

4. **التنسيق:**
   - الحفاظ على جميع تنسيقات Markdown
   - الحفاظ على بنية الجداول
   - عدم إضافة أي ملاحظة أو رابط في بداية الوثيقة (سيتم إضافتها تلقائيًا)

الرجاء ترجمة الوثيقة التالية إلى العربية، دون تضمين الملاحظة الأولية للوثيقة:

> تم إنشاء هذه الوثائق بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/FluxProImageNode/en.md)

يقوم بإنشاء الصور بشكل متزامن بناءً على الموجه (prompt) والدقة. تستخدم هذه العقدة نموذج Flux 1.1 Pro لإنشاء الصور عن طريق إرسال طلبات إلى نقطة نهاية API والانتظار حتى اكتمال الاستجابة قبل إرجاع الصورة المُنشأة.

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
| --- | --- | --- | --- | --- |
| `prompt` | الموجه (prompt) لإنشاء الصورة (القيمة الافتراضية: سلسلة فارغة) | STRING | نعم | - |
| `prompt_upsampling` | ما إذا كان سيتم إجراء تحسين للدقة على الموجه (prompt). إذا كان نشطًا، يقوم بتعديل الموجه تلقائيًا لتوليد أكثر إبداعًا، ولكن النتائج غير حتمية (نفس البذرة لن تنتج نفس النتيجة تمامًا). (القيمة الافتراضية: False) | BOOLEAN | نعم | - |
| `width` | عرض الصورة بالبكسل (القيمة الافتراضية: 1024، الخطوة: 32) | INT | نعم | 256-1440 |
| `height` | ارتفاع الصورة بالبكسل (القيمة الافتراضية: 768، الخطوة: 32) | INT | نعم | 256-1440 |
| `seed` | البذرة العشوائية المستخدمة لإنشاء الضوضاء. (القيمة الافتراضية: 0) | INT | نعم | 0-18446744073709551615 |
| `image_prompt` | صورة مرجعية اختيارية لتوجيه عملية الإنشاء | IMAGE | لا | - |

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `output` | الصورة المُنشأة التي تم إرجاعها من API | IMAGE |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/FluxProImageNode/ar.md)

---
**Source fingerprint (SHA-256):** `89316d84f364854541157b5b60bae3d4e25024bd4af61a47a1748c6671b463c1`
