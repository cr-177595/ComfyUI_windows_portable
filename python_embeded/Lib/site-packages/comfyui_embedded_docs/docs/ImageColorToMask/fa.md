# تبدیل رنگ به ماسک

گره `ImageColorToMask` برای تبدیل یک رنگ مشخص در تصویر به ماسک طراحی شده است. این گره یک تصویر و یک رنگ هدف را پردازش کرده و ماسکی تولید می‌کند که در آن رنگ مشخص شده برجسته می‌شود و عملیاتی مانند جداسازی مبتنی بر رنگ یا ایزوله کردن اشیاء را تسهیل می‌کند.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده |
| --- | --- | --- |
| `تصویر` | پارامتر `تصویر` نشان‌دهنده تصویر ورودی برای پردازش است. این پارامتر برای تعیین نواحی از تصویر که با رنگ مشخص شده مطابقت دارند و باید به ماسک تبدیل شوند، ضروری است. | `IMAGE` |
| `رنگ` | پارامتر `رنگ` رنگ هدف در تصویر را مشخص می‌کند که باید به ماسک تبدیل شود. این پارامتر نقش کلیدی در شناسایی نواحی رنگی خاص دارد که در ماسک خروجی برجسته می‌شوند. | `INT` |

## خروجی‌ها

| پارامتر | توضیحات | نوع داده |
| --- | --- | --- |
| `mask` | خروجی یک ماسک است که نواحی از تصویر ورودی را که با رنگ مشخص شده مطابقت دارند، برجسته می‌کند. این ماسک می‌تواند برای پردازش‌های بعدی تصویر، مانند بخش‌بندی یا ایزوله کردن اشیاء، استفاده شود. | `MASK` |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ImageColorToMask/fa.md)
