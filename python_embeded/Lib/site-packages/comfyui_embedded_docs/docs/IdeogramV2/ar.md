# إيديوغرام الإصدار 2

هذا هو النص المترجم إلى العربية مع الالتزام بالقواعد المحددة:

تقوم عقدة Ideogram V2 بإنشاء الصور باستخدام نموذج الذكاء الاصطناعي Ideogram V2. تستقبل العقدة نصوصًا توجيهية (برومبت) وإعدادات توليد متنوعة لإنشاء الصور عبر خدمة API. تدعم العقدة نسب أبعاد ودقة وخيارات أنماط مختلفة لتخصيص الصور الناتجة.

## المدخلات

| المعامل | الوصف | نوع البيانات | إجباري | النطاق |
| --- | --- | --- | --- | --- |
| `الموجه` | النص التوجيهي لتوليد الصورة (القيمة الافتراضية: سلسلة فارغة) | STRING | نعم | - |
| `تيربو` | تفعيل الوضع السريع (توليد أسرع، جودة أقل محتملة) (القيمة الافتراضية: False) | BOOLEAN | لا | - |
| `نسبة العرض إلى الارتفاع` | نسبة الأبعاد لتوليد الصورة. يتم تجاهلها إذا لم تكن الدقة مضبوطة على AUTO. (القيمة الافتراضية: "1:1") | COMBO | لا | "1:1"<br>"16:9"<br>"9:16"<br>"4:3"<br>"3:4"<br>"3:2"<br>"2:3" |
| `الدقة` | دقة الصورة للتوليد. إذا لم تكن مضبوطة على AUTO، فإنها تتجاوز إعداد نسبة الأبعاد. (القيمة الافتراضية: "Auto") | COMBO | لا | "Auto"<br>"1024x1024"<br>"1152x896"<br>"896x1152"<br>"1216x832"<br>"832x1216"<br>"1344x768"<br>"768x1344"<br>"1536x640"<br>"640x1536" |
| `خيار الموجه السحري` | تحديد ما إذا كان سيتم استخدام MagicPrompt في التوليد (القيمة الافتراضية: "AUTO") | COMBO | لا | "AUTO"<br>"ON"<br>"OFF" |
| `البذرة` | البذرة العشوائية للتوليد (القيمة الافتراضية: 0) | INT | لا | 0-2147483647 |
| `نوع الأسلوب` | نوع النمط للتوليد (لـ V2 فقط) (القيمة الافتراضية: "NONE") | COMBO | لا | "AUTO"<br>"GENERAL"<br>"REALISTIC"<br>"DESIGN"<br>"RENDER_3D"<br>"ANIME" |
| `الموجه السلبي` | وصف ما يجب استبعاده من الصورة (القيمة الافتراضية: سلسلة فارغة) | STRING | لا | - |
| `عدد الصور` | عدد الصور المراد توليدها (القيمة الافتراضية: 1) | INT | لا | 1-8 |

**ملاحظة:** عندما لا تكون `resolution` مضبوطة على "Auto"، فإنها تتجاوز إعداد `aspect_ratio`. المعامل `num_images` له حد أقصى يبلغ 8 صور لكل عملية توليد.

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `output` | الصورة (الصور) المُنشأة من نموذج Ideogram V2 | IMAGE |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/IdeogramV2/ar.md)

---
**Source fingerprint (SHA-256):** `c0ba21cb62ad75212c960e2bf6730a39c6479c7389a58c50968c66cc8964f5e3`
