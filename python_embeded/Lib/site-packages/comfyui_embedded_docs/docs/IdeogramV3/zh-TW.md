# Ideogram V3

此節點使用 Ideogram V3 模型生成圖像。它支援從文字提示進行常規圖像生成，以及在同時提供圖像和遮罩時進行圖像編輯。該節點提供多種控制選項，包括長寬比、解析度、生成速度和可選的角色參考圖像。

## 輸入

| 參數 | 說明 | 資料類型 | 必要 | 範圍 |
| --- | --- | --- | --- | --- |
| `提示詞` | 用於圖像生成或編輯的提示詞（預設：空） | STRING | 是 | - |
| `參考影像` | 用於圖像編輯的參考圖像（可選） | IMAGE | 否 | - |
| `遮罩` | 用於修復的遮罩（白色區域將被替換）（可選） | MASK | 否 | - |
| `長寬比` | 圖像生成的長寬比。若解析度未設為自動則忽略此設定（預設："1:1"） | COMBO | 否 | "1:1"<br>"16:9"<br>"9:16"<br>"4:3"<br>"3:4"<br>"3:2"<br>"2:3" |
| `解析度` | 圖像生成的解析度。若未設為自動，則此設定會覆蓋長寬比設定（預設："Auto"） | COMBO | 否 | "Auto"<br>"1024x1024"<br>"1152x896"<br>"896x1152"<br>"1216x832"<br>"832x1216"<br>"1344x768"<br>"768x1344"<br>"1536x640"<br>"640x1536" |
| `MagicPrompt 選項` | 決定生成時是否使用 MagicPrompt（預設："AUTO"） | COMBO | 否 | "AUTO"<br>"ON"<br>"OFF" |
| `種子值` | 用於生成的隨機種子（預設：0） | INT | 否 | 0-2147483647 |
| `影像數量` | 要生成的圖像數量（預設：1） | INT | 否 | 1-8 |
| `生成速度` | 控制生成速度與品質之間的權衡（預設："DEFAULT"） | COMBO | 否 | "DEFAULT"<br>"TURBO"<br>"QUALITY" |
| `角色圖片` | 用作角色參考的圖像 | IMAGE | 否 | - |
| `角色遮罩` | 角色參考圖像的可選遮罩 | MASK | 否 | - |

**參數限制：**

- 當同時提供 `image` 和 `mask` 時，節點會切換到編輯模式
- 如果只提供了 `image` 或 `mask` 其中一個，將會發生錯誤
- `character_mask` 需要 `character_image` 同時存在
- 當 `resolution` 未設為 "Auto" 時，`aspect_ratio` 參數將被忽略
- 遮罩中的白色區域將在修復過程中被替換
- 角色遮罩和角色圖像必須具有相同尺寸

## 輸出

| 輸出名稱 | 說明 | 資料類型 |
| --- | --- | --- |
| `output` | 生成或編輯後的圖像 | IMAGE |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/IdeogramV3/zh-TW.md)

---
**Source fingerprint (SHA-256):** `0d0058cc8483c453100d8d9dfcb9a31ae5e686f38ced77ed7e472cd083c3464b`
