# إيديوغرام الإصدار 3

عقدة Ideogram V3 تُنشئ الصور باستخدام نموذج Ideogram V3. تدعم العقدة كلاً من توليد الصور العادي من النصوص الوصفية وتحرير الصور عند توفير كل من الصورة والقناع. توفر العقدة تحكمات متنوعة لنسبة الأبعاد، الدقة، سرعة التوليد، وصور مرجعية اختيارية للشخصيات.

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
| --- | --- | --- | --- | --- |
| `الموجه` | النص الوصفي لتوليد الصورة أو تحريرها (الافتراضي: فارغ) | STRING | نعم | - |
| `الصورة` | صورة مرجعية اختيارية لتحرير الصورة | IMAGE | لا | - |
| `القناع` | قناع اختياري للرسم الداخلي (سيتم استبدال المناطق البيضاء) | MASK | لا | - |
| `نسبة العرض إلى الارتفاع` | نسبة الأبعاد لتوليد الصورة. يتم تجاهلها إذا لم تكن الدقة مضبوطة على تلقائي (الافتراضي: "1:1") | COMBO | لا | "1:1"<br>"16:9"<br>"9:16"<br>"4:3"<br>"3:4"<br>"3:2"<br>"2:3" |
| `الدقة` | دقة توليد الصورة. إذا لم تكن مضبوطة على تلقائي، فإنها تتجاوز إعداد نسبة الأبعاد (الافتراضي: "Auto") | COMBO | لا | "Auto"<br>"1024x1024"<br>"1152x896"<br>"896x1152"<br>"1216x832"<br>"832x1216"<br>"1344x768"<br>"768x1344"<br>"1536x640"<br>"640x1536" |
| `خيار الموجه السحري` | تحديد ما إذا كان سيتم استخدام MagicPrompt في التوليد (الافتراضي: "AUTO") | COMBO | لا | "AUTO"<br>"ON"<br>"OFF" |
| `البذرة` | البذرة العشوائية للتوليد (الافتراضي: 0) | INT | لا | 0-2147483647 |
| `عدد الصور` | عدد الصور المراد توليدها (الافتراضي: 1) | INT | لا | 1-8 |
| `سرعة العرض` | يتحكم في المفاضلة بين سرعة التوليد والجودة (الافتراضي: "DEFAULT") | COMBO | لا | "DEFAULT"<br>"TURBO"<br>"QUALITY" |
| `صورة الشخصية` | صورة لاستخدامها كمرجع للشخصية | IMAGE | لا | - |
| `قناع الشخصية` | قناع اختياري لصورة مرجع الشخصية | MASK | لا | - |

**قيود المعاملات:**

- عند توفير كل من `image` و `mask`، تتحول العقدة إلى وضع التحرير
- إذا تم توفير واحد فقط من `image` أو `mask`، سيحدث خطأ
- `character_mask` يتطلب وجود `character_image`
- يتم تجاهل معامل `aspect_ratio` عندما لا تكون `resolution` مضبوطة على "Auto"
- سيتم استبدال المناطق البيضاء في القناع أثناء الرسم الداخلي
- يجب أن يكون قناع الشخصية وصورة الشخصية بنفس الحجم

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `output` | الصورة(الصور) المُنشأة أو المُحررة | IMAGE |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/IdeogramV3/ar.md)

---
**Source fingerprint (SHA-256):** `0d0058cc8483c453100d8d9dfcb9a31ae5e686f38ced77ed7e472cd083c3464b`
