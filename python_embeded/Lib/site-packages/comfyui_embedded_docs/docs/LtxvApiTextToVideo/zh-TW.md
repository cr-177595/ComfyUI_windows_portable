# LTXV 文字轉影片

## 概述

LTXV 文字轉影片節點可根據文字描述生成專業品質的影片。它連接到外部 API，以建立具有可自訂時長、解析度和幀率的影片。您也可以選擇讓 AI 生成的音訊添加到影片中。

## 輸入

| 參數 | 說明 | 資料類型 | 必要 | 範圍 |
| --- | --- | --- | --- | --- |
| `模型` | 用於影片生成的 AI 模型。可用的模型會根據原始碼中的 `MODELS_MAP` 進行對應。 | COMBO | 是 | `"LTX-2 (Fast)"`<br>`"LTX-2 (Quality)"`<br>`"LTX-2 (Turbo)"` |
| `提示詞` | AI 將用來生成影片的文字描述。此欄位支援多行文字。 | STRING | 是 | - |
| `持續時間` | 生成影片的長度，以秒為單位（預設值：8）。 | COMBO | 是 | `6`<br>`8`<br>`10`<br>`12`<br>`14`<br>`16`<br>`18`<br>`20` |
| `解析度` | 輸出影片的像素尺寸（寬度 x 高度）。 | COMBO | 是 | `"1920x1080"`<br>`"2560x1440"`<br>`"3840x2160"` |
| `幀率` | 影片的每秒幀數（預設值：25）。 | COMBO | 是 | `25`<br>`50` |
| `生成音訊` | 啟用時，生成的影片將包含與場景匹配的 AI 生成音訊（預設值：False）。 | BOOLEAN | 否 | - |

**重要限制：**

* `prompt` 的長度必須在 1 到 10,000 個字元之間。
* 如果您選擇的 `duration` 大於 10 秒，則必須使用 `"LTX-2 (Fast)"` 模型、解析度為 `"1920x1080"`，且 `fps` 為 `25`。較長的影片需要此組合。

## 輸出

| 輸出名稱 | 說明 | 資料類型 |
| --- | --- | --- |
| `output` | 生成的影片檔案。 | VIDEO |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LtxvApiTextToVideo/zh-TW.md)

---
**Source fingerprint (SHA-256):** `a0c16995a07d879113bd3ca8fea64be414feee96bd8293a3e7737ede7d30e11d`
