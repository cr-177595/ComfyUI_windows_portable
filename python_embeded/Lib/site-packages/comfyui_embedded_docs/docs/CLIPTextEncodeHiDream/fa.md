# CLIPTextEncodeHiDream

گره CLIPTextEncodeHiDream چهار ورودی متنی مجزا را با استفاده از مدل‌های زبانی مختلف (CLIP-L، CLIP-G، T5-XXL و LLaMA) پردازش کرده و آن‌ها را در یک خروجی شرطی‌سازی واحد ترکیب می‌کند. این گره هر ورودی متنی را با مدل متناظر آن توکن‌سازی کرده و با استفاده از رویکرد رمزگذاری زمان‌بندی‌شده، آن‌ها را با یکدیگر رمزگذاری می‌کند. این کار با بهره‌گیری هم‌زمان از چندین مدل زبانی، شرطی‌سازی متنی پیچیده‌تری را امکان‌پذیر می‌سازد.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
| --- | --- | --- | --- | --- |
| `clip` | مدل CLIP مورد استفاده برای توکن‌سازی و رمزگذاری | CLIP | بله | - |
| `clip_l` | ورودی متنی برای پردازش مدل CLIP-L. از متن چندخطی و پرامپت‌های پویا پشتیبانی می‌کند. | STRING | بله | - |
| `clip_g` | ورودی متنی برای پردازش مدل CLIP-G. از متن چندخطی و پرامپت‌های پویا پشتیبانی می‌کند. | STRING | بله | - |
| `t5xxl` | ورودی متنی برای پردازش مدل T5-XXL. از متن چندخطی و پرامپت‌های پویا پشتیبانی می‌کند. | STRING | بله | - |
| `llama` | ورودی متنی برای پردازش مدل LLaMA. از متن چندخطی و پرامپت‌های پویا پشتیبانی می‌کند. | STRING | بله | - |

**توجه:** هر چهار ورودی متنی (`clip_l`، `clip_g`، `t5xxl` و `llama`) برای عملکرد صحیح الزامی هستند، زیرا هر یک از طریق فرآیند رمزگذاری زمان‌بندی‌شده به خروجی نهایی شرطی‌سازی کمک می‌کنند.

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
| --- | --- | --- |
| `CONDITIONING` | خروجی شرطی‌سازی ترکیبی از تمام ورودی‌های متنی پردازش‌شده که با استفاده از روش رمزگذاری زمان‌بندی‌شده رمزگذاری شده‌اند | CONDITIONING |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/CLIPTextEncodeHiDream/fa.md)

---
**Source fingerprint (SHA-256):** `51d117d82a9d833f095e874bf442d5cf8c46a12313fda6b98e628fa988797565`
