# Случайное кадрирование изображений

Узел "Random Crop Images" (Случайная обрезка изображений) случайным образом выбирает прямоугольный участок из каждого входного изображения и обрезает его до заданной ширины и высоты. Это часто используется для аугментации данных с целью создания вариаций обучающих изображений. Случайное положение обрезки определяется значением seed, что позволяет воспроизвести ту же самую обрезку.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `image` | Изображение, которое будет обрезано. | IMAGE | Да | - |
| `width` | Ширина области обрезки (по умолчанию: 512). | INT | Нет | 1 - 8192 |
| `height` | Высота области обрезки (по умолчанию: 512). | INT | Нет | 1 - 8192 |
| `seed` | Число, используемое для управления случайным положением обрезки (по умолчанию: 0). | INT | Нет | 0 - 18446744073709551615 |

**Примечание:** Параметры `width` и `height` должны быть меньше или равны размерам входного изображения. Если указанный размер превышает размер изображения, обрезка будет ограничена границами изображения.

## Выходные параметры

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `image` | Результирующее изображение после применения случайной обрезки. | IMAGE |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/RandomCropImages/ru.md)

---
**Source fingerprint (SHA-256):** `bc4aca8cc63bde28fee906a92463b73436ba48ba69d7c1ff13881ac900e252a8`
