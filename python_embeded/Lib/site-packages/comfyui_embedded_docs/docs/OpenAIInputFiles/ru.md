# Файлы ввода OpenAI ChatGPT

Загружает и форматирует входные файлы для API OpenAI. Этот узел подготавливает текстовые (.txt) и PDF (.pdf) файлы для включения в качестве контекстных входных данных для узла OpenAI Chat Node. Файлы будут прочитаны моделью OpenAI при генерации ответа. Несколько узлов OpenAI Input Files можно объединить в цепочку, чтобы включить несколько файлов в одно сообщение.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `файл` | Входные файлы для включения в качестве контекста для модели. На данный момент принимаются только текстовые (.txt) и PDF (.pdf) файлы. Размер файлов не должен превышать 32 МБ. | COMBO | Да | Доступно несколько опций (все файлы .txt и .pdf в папке input размером менее 32 МБ) |
| `OPENAI_INPUT_FILES` | Дополнительный файл(ы) для объединения в пакет с файлом, загруженным из этого узла. Позволяет объединять входные файлы в цепочку, чтобы одно сообщение могло содержать несколько входных файлов. | OPENAI_INPUT_FILES | Нет | Н/Д |

**Ограничения по файлам:**

- Поддерживаются только файлы .txt и .pdf
- Максимальный размер файла: 32 МБ
- Файлы загружаются из папки input ComfyUI

## Выходные данные

| Имя выходного параметра | Описание | Тип данных |
| --- | --- | --- |
| `OPENAI_INPUT_FILES` | Отформатированные входные файлы, готовые к использованию в качестве контекста для вызовов API OpenAI. | OPENAI_INPUT_FILES |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/OpenAIInputFiles/ru.md)

---
**Source fingerprint (SHA-256):** `e5e92f6628072da9af787867e38c89dde3db853b7289ef6c607a066cd04c1cc9`
