# DeprecatedCheckpointLoader

عقدة تحميل نقاط التفتيش (CheckpointLoader) مصممة لعمليات التحميل المتقدمة، وتحديدًا لتحميل نقاط تفتيش النماذج مع إعداداتها. تسهل هذه العقدة استرجاع مكونات النموذج اللازمة لتهيئة وتشغيل النماذج التوليدية، بما في ذلك الإعدادات ونقاط التفتيش من المسارات المحددة.

## المدخلات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `config_name` | يحدد اسم ملف الإعدادات الذي سيتم استخدامه. هذا أمر بالغ الأهمية لتحديد معاملات النموذج وإعداداته، مما يؤثر على سلوك النموذج وأدائه. | COMBO[STRING] |
| `ckpt_name` | يشير إلى اسم ملف نقطة التفتيش الذي سيتم تحميله. يؤثر هذا بشكل مباشر على حالة النموذج الذي تتم تهيئته، مما يؤثر على أوزانه الأولية وتحيزاته. | COMBO[STRING] |

## المخرجات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `model` | يمثل النموذج الأساسي المحمّل من نقطة التفتيش، الجاهز لمزيد من العمليات أو الاستدلال. | MODEL |
| `clip` | يوفر مكون نموذج CLIP، إذا كان متاحًا ومطلوبًا، والمحمّل من نقطة التفتيش. | CLIP |
| `vae` | يسلم مكون نموذج VAE، إذا كان متاحًا ومطلوبًا، والمحمّل من نقطة التفتيش. | VAE |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/DeprecatedCheckpointLoader/ar.md)
