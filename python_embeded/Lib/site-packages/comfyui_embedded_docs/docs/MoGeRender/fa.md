# MoGe Render

## نمای کلی

این گره یک بسته MOGE_GEOMETRY (تولیدشده توسط گره تخمین عمق/نرمال MoGe) را دریافت کرده و آن را به فرمت تصویر استاندارد رندر می‌کند. می‌توانید انتخاب کنید که خروجی به صورت نقشه عمق، نقشه عمق رنگی، نقشه نرمال یا ماسک باشد.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | ضروری | محدوده |
| --- | --- | --- | --- | --- |
| `moge_geometry` | بسته داده‌های هندسی از گره تخمین MoGe. | MOGE_GEOMETRY | بله | نامشخص |
| `output` | نوع تصویری که از داده‌های هندسی رندر می‌شود. DirectX در مقابل OpenGL قرارداد کانال سبز نقشه نرمال را کنترل می‌کند. DirectX: سبز = -Y به سمت پایین (Unreal). OpenGL: سبز = +Y به سمت بالا (Blender, Substance, Unity, glTF). (پیش‌فرض: "depth") | COMBO | بله | `"depth"`<br>`"depth_colored"`<br>`"normal_opengl"`<br>`"normal_directx"`<br>`"mask"` |

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
| --- | --- | --- |
| `IMAGE` | تصویر رندر شده به صورت دسته‌ای از تنسورهای RGB. محتوا به حالت `output` بستگی دارد: نقشه عمق خاکستری، نقشه عمق رنگی، نقشه نرمال یا ماسک. | IMAGE |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/MoGeRender/fa.md)

---
**Source fingerprint (SHA-256):** `45ba499e746ce46f9b6f7773e3218bcf80ad2e8d65940b38e248cc2f20c8b2fe`
