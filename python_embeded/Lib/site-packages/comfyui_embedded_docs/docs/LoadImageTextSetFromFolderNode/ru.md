# LoadImageTextSetFromFolderNode

Загружает пакет изображений и соответствующие им текстовые подписи из указанной директории для целей обучения. Узел автоматически ищет файлы изображений и связанные с ними файлы текстовых подписей, обрабатывает изображения в соответствии с заданными настройками изменения размера и кодирует подписи с помощью предоставленной модели CLIP.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `folder` | Папка для загрузки изображений. | STRING | Да | - |
| `clip` | Модель CLIP, используемая для кодирования текста. | CLIP | Да | - |
| `resize_method` | Метод изменения размера изображений (по умолчанию: "None"). | COMBO | Нет | "None"<br>"Stretch"<br>"Crop"<br>"Pad" |
| `width` | Ширина для изменения размера изображений. -1 означает использование исходной ширины (по умолчанию: -1). | INT | Нет | от -1 до 10000 |
| `height` | Высота для изменения размера изображений. -1 означает использование исходной высоты (по умолчанию: -1). | INT | Нет | от -1 до 10000 |

**Примечание:** Входной параметр CLIP должен быть действительным и не может быть None. Если модель CLIP получена из узла загрузки контрольной точки, убедитесь, что контрольная точка содержит действительную модель CLIP или модель текстового кодировщика.

**Примечание о структуре папок:** Узел поддерживает структуру папок kohya-ss/sd-scripts. Если имя подпапки начинается с числа, за которым следует символ подчеркивания (например, `5_myclass`), это число используется как количество повторений, и изображения внутри этой подпапки будут загружены соответствующее количество раз.

## Выходные параметры

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `IMAGE` | Пакет загруженных и обработанных изображений. | IMAGE |
| `CONDITIONING` | Закодированные данные conditioning из текстовых подписей. | CONDITIONING |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LoadImageTextSetFromFolderNode/ru.md)

---
**Source fingerprint (SHA-256):** `ffd6399783fc281a58bae811112d9ecacb51ab8ea3b512befa9b9fab2c6860de`
