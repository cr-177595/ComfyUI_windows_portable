# الكامن من الدفعة

هذه العقدة مصممة لاستخراج مجموعة فرعية محددة من العينات الكامنة من دفعة معينة بناءً على فهرس الدفعة والطول المحددين. وهي تتيح معالجة انتقائية للعينات الكامنة، مما يسهل العمليات على أجزاء أصغر من الدفعة لتحقيق الكفاءة أو المعالجة المستهدفة.

## المدخلات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `samples` | مجموعة العينات الكامنة التي سيتم استخراج مجموعة فرعية منها. هذا المعامل ضروري لتحديد الدفعة المصدر للعينات المراد معالجتها. | `LATENT` |
| `batch_index` | يحدد فهرس البداية داخل الدفعة الذي ستبدأ منه المجموعة الفرعية من العينات. يتيح هذا المعامل الاستخراج المستهدف للعينات من مواضع محددة في الدفعة. | `INT` |
| `length` | يحدد عدد العينات المراد استخراجها من فهرس البداية المحدد. يتحكم هذا المعامل في حجم المجموعة الفرعية المراد معالجتها، مما يسمح بمعالجة مرنة لأجزاء الدفعة. | `INT` |

## المخرجات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `latent` | المجموعة الفرعية المستخرجة من العينات الكامنة، والتي أصبحت متاحة الآن لمزيد من المعالجة أو التحليل. | `LATENT` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LatentFromBatch/ar.md)
