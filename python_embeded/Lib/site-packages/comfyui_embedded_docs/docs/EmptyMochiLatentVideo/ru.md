# Пустой MochiLatentVideo

Узел EmptyMochiLatentVideo создает пустой тензор скрытого видео с заданными размерами. Он генерирует заполненное нулями скрытое представление, которое можно использовать в качестве отправной точки для рабочих процессов генерации видео. Узел позволяет задать ширину, высоту, длину и размер пакета для тензора скрытого видео.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `ширина` | Ширина скрытого видео в пикселях (по умолчанию: 848, должна делиться на 16) | INT | Да | от 16 до MAX_RESOLUTION |
| `высота` | Высота скрытого видео в пикселях (по умолчанию: 480, должна делиться на 16) | INT | Да | от 16 до MAX_RESOLUTION |
| `длина` | Количество кадров в скрытом видео (по умолчанию: 25, должно делиться на 6 после вычитания 1) | INT | Да | от 7 до MAX_RESOLUTION |
| `размер_пакета` | Количество скрытых видео для генерации в одном пакете (по умолчанию: 1) | INT | Нет | от 1 до 4096 |

**Примечание:** Фактические размеры скрытого пространства вычисляются как ширина/8 и высота/8, а временное измерение вычисляется как ((длина - 1) // 6) + 1. Параметр `length` должен удовлетворять условию, что `(length - 1)` делится на 6, то есть допустимые значения: 7, 13, 19, 25 и т.д.

## Выходные данные

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `samples` | Пустой тензор скрытого видео с заданными размерами, содержащий все нули | LATENT |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/EmptyMochiLatentVideo/ru.md)

---
**Source fingerprint (SHA-256):** `6876a739355b2dcde42f8c02eb67405678798b818865ec1a73e19076b738554b`
