# توسيع الصورة للرسم الخارجي

تم تصميم هذه العقدة لإعداد الصور لعملية التمديد الخارجي (Outpainting) عن طريق إضافة حواف حولها. تقوم بضبط أبعاد الصورة لضمان التوافق مع خوارزميات التمديد الخارجي، مما يسهل توليد مساحات ممتدة من الصورة تتجاوز الحدود الأصلية.

## المدخلات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `الصورة` | مدخل "الصورة" هو الصورة الأساسية التي سيتم إعدادها لعملية التمديد الخارجي، وتعمل كقاعدة لعمليات إضافة الحواف. | `IMAGE` |
| `اليسار` | يحدد مقدار الحافة المراد إضافتها إلى الجانب الأيسر من الصورة، مما يؤثر على المساحة الموسعة للتمديد الخارجي. | `INT` |
| `الأعلى` | يحدد مقدار الحافة المراد إضافتها إلى أعلى الصورة، مما يؤثر على التمدد الرأسي للتمديد الخارجي. | `INT` |
| `اليمين` | يحدد مقدار الحافة المراد إضافتها إلى الجانب الأيمن من الصورة، مما يؤثر على التمدد الأفقي للتمديد الخارجي. | `INT` |
| `الأسفل` | يشير إلى مقدار الحافة المراد إضافتها إلى أسفل الصورة، مما يساهم في التمدد الرأسي للتمديد الخارجي. | `INT` |
| `التدرج` | يتحكم في نعومة الانتقال بين الصورة الأصلية والحواف المضافة، مما يعزز التكامل البصري لعملية التمديد الخارجي. | `INT` |

## المخرجات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `الصورة` | تمثل الصورة الناتجة "الصورة" الصورة ذات الحواف المضافة، الجاهزة لعملية التمديد الخارجي. | `IMAGE` |
| `mask` | يشير القناع الناتج "القناع" إلى مناطق الصورة الأصلية والحواف المضافة، وهو مفيد لتوجيه خوارزميات التمديد الخارجي. | `MASK` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ImagePadForOutpaint/ar.md)
