# الضرب الكامن

عقدة ضرب الكمون (LatentMultiply) مصممة لتوسيع نطاق التمثيل الكموني للعينات بواسطة مضاعف محدد. تتيح هذه العملية ضبط شدة أو حجم الميزات داخل الفضاء الكموني، مما يتيح الضبط الدقيق للمحتوى المُنشأ أو استكشاف الاختلافات ضمن اتجاه كموني معين.

## المدخلات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `عينات` | يمثل معامل 'samples' التمثيلات الكمونية المراد توسيع نطاقها. وهو ضروري لتحديد بيانات الإدخال التي سيتم تنفيذ عملية الضرب عليها. | `LATENT` |
| `المضاعف` | يحدد معامل 'multiplier' عامل القياس الذي سيتم تطبيقه على العينات الكمونية. يلعب دورًا رئيسيًا في ضبط حجم الميزات الكمونية، مما يتيح تحكمًا دقيقًا في المخرجات المُنشأة. | `FLOAT` |

## المخرجات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `latent` | المخرجات هي نسخة معدلة من العينات الكمونية المدخلة، موسعة النطاق بواسطة المضاعف المحدد. يتيح ذلك استكشاف الاختلافات داخل الفضاء الكموني عن طريق ضبط شدة ميزاته. | `LATENT` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LatentMultiply/ar.md)
