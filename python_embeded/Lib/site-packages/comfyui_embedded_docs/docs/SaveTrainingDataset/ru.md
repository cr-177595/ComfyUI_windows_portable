# Сохранить обучающий набор данных

Вот перевод документации на русский язык:

Этот узел сохраняет подготовленный обучающий набор данных на жесткий диск вашего компьютера. Он принимает закодированные данные, которые включают скрытые представления изображений (latents) и соответствующие им текстовые условия (conditioning), и организует их в несколько меньших файлов, называемых шардами (shards), для упрощения управления. Узел автоматически создает папку в вашем выходном каталоге и сохраняет как файлы данных, так и файл метаданных, описывающий набор данных.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `latents` | Список словарей скрытых представлений из узла MakeTrainingDataset. | LATENT | Да | Н/Д |
| `conditioning` | Список списков условий из узла MakeTrainingDataset. | CONDITIONING | Да | Н/Д |
| `folder_name` | Имя папки для сохранения набора данных (внутри выходного каталога). (по умолчанию: "training_dataset") | STRING | Нет | Н/Д |
| `shard_size` | Количество образцов в одном файле шарда. (по умолчанию: 1000) | INT | Нет | от 1 до 100000 |

**Примечание:** Количество элементов в списке `latents` должно точно совпадать с количеством элементов в списке `conditioning`. Узел выдаст ошибку, если эти количества не совпадают.

## Выходные параметры

Этот узел не создает никаких выходных данных. Его функция заключается в сохранении файлов на ваш диск.

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/SaveTrainingDataset/ru.md)

---
**Source fingerprint (SHA-256):** `1b0108be7362c0cb8ba16ffbf94cf42be2d04159aacbabe1ff0890083d1733b3`
