# BatchImagesMasksLatentsNode

Вот перевод документации на русский язык:

Узел Batch Images/Masks/Latents объединяет несколько входных данных одного типа в единый пакет. Он автоматически определяет, являются ли входные данные изображениями, масками или латентными представлениями, и использует соответствующий метод пакетной обработки. Это полезно для подготовки нескольких элементов к обработке узлами, которые принимают пакетные входные данные.

## Входные данные

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `inputs` | Динамический список входных данных для объединения в пакет. Можно добавить от 1 до 50 элементов. Все элементы должны быть одного типа (все изображения, все маски или все латентные представления). | IMAGE, MASK или LATENT | Да | от 1 до 50 входов |

**Примечание:** Узел автоматически определяет тип данных (IMAGE, MASK или LATENT) на основе первого элемента в списке `inputs`. Все последующие элементы должны соответствовать этому типу. Узел выдаст ошибку, если попытаться смешивать разные типы данных.

## Выходные данные

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `output` | Единый пакетный выходной результат. Тип данных соответствует типу входных данных (пакетный IMAGE, пакетный MASK или пакетный LATENT). | IMAGE, MASK или LATENT |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/BatchImagesMasksLatentsNode/ru.md)

---
**Source fingerprint (SHA-256):** `6f3037bc00fd8526f42ad2d79a0f27434f58bd6dd0338a585cc707a771ac0989`
