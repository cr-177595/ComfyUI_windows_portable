# ترميز VAE (للتلوين)

تم تصميم هذه العقدة لتشفير الصور إلى تمثيل كامن (latent representation) مناسب لمهام الترميم (inpainting)، مع تضمين خطوات معالجة مسبقة إضافية لتعديل الصورة المدخلة والقناع (mask) لضمان التشفير الأمثل بواسطة نموذج VAE.

## المدخلات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `بكسلات` | الصورة المدخلة المراد تشفيرها. تخضع هذه الصورة لمعالجة مسبقة وإعادة تحجيم لتتناسب مع أبعاد الإدخال المتوقعة لنموذج VAE قبل التشفير. | `IMAGE` |
| `vae` | نموذج VAE المستخدم لتشفير الصورة إلى تمثيلها الكامن. يلعب دورًا حاسمًا في عملية التحويل، حيث يحدد جودة وخصائص الفضاء الكامن الناتج. | VAE |
| `قناع` | قناع يحدد مناطق الصورة المدخلة التي سيتم ترميمها. يُستخدم لتعديل الصورة قبل التشفير، مما يضمن تركيز نموذج VAE على المناطق ذات الصلة. | `MASK` |
| `توسيع القناع بمقدار` | يحدد مقدار توسيع قناع الترميم لضمان انتقالات سلسة في الفضاء الكامن. تؤدي القيمة الأكبر إلى زيادة المساحة المتأثرة بعملية الترميم. | `INT` |

## المخرجات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `latent` | يشمل المخرج التمثيل الكامن المشفر للصورة وقناع الضوضاء (noise mask)، وكلاهما ضروري لمهام الترميم اللاحقة. | `LATENT` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/VAEEncodeForInpaint/ar.md)
