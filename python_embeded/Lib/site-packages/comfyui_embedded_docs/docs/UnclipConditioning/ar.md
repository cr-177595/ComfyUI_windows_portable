# تكييف unCLIP

هذه العقدة مصممة لدمج مخرجات رؤية CLIP في عملية التكييف، مع ضبط تأثير هذه المخرجات بناءً على معاملات القوة المحددة وزيادة الضوضاء. وهي تُثري التكييف بالسياق البصري، مما يُعزز عملية التوليد.

## المدخلات

| المعامل | الوصف | نوع Comfy |
| --- | --- | --- |
| `التكييف` | بيانات التكييف الأساسية التي ستُضاف إليها مخرجات رؤية CLIP، وتعمل كأساس لإجراء المزيد من التعديلات. | `CONDITIONING` |
| `ناتج رؤية الكليب` | المخرجات من نموذج رؤية CLIP، والتي توفر سياقًا بصريًا يتم دمجه في التكييف. | `CLIP_VISION_OUTPUT` |
| `القوة` | يُحدد شدة تأثير مخرجات رؤية CLIP على التكييف. | `FLOAT` |
| `زيادة الضجيج` | يُحدد مستوى زيادة الضوضاء الذي سيتم تطبيقه على مخرجات رؤية CLIP قبل دمجها في التكييف. | `FLOAT` |

## المخرجات

| المعامل | الوصف | نوع Comfy |
| --- | --- | --- |
| `التكييف` | بيانات التكييف المُثراة، والتي تحتوي الآن على مخرجات رؤية CLIP المدمجة مع تطبيق القوة وزيادة الضوضاء. | `CONDITIONING` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/unCLIPConditioning/ar.md)
