# Gemini Input Files

Загружает и форматирует входные файлы для использования с Gemini API. Этот узел позволяет пользователям включать текстовые файлы (.txt) и PDF-файлы (.pdf) в качестве входного контекста для модели Gemini. Файлы преобразуются в формат, требуемый API, и могут быть объединены в цепочку для включения нескольких файлов в один запрос.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `file` | Входные файлы, включаемые в качестве контекста для модели. На данный момент принимаются только текстовые файлы (.txt) и PDF-файлы (.pdf). Размер файлов не должен превышать максимально допустимый лимит. | COMBO | Да | Доступно несколько опций |
| `GEMINI_INPUT_FILES` | Дополнительный файл(ы) для объединения в пакет с файлом, загруженным через этот узел. Позволяет объединять входные файлы в цепочку, чтобы одно сообщение могло содержать несколько входных файлов. | GEMINI_INPUT_FILES | Нет | Н/Д |

**Примечание:** Параметр `file` отображает только текстовые файлы (.txt) и PDF-файлы (.pdf), размер которых не превышает максимально допустимый лимит. Файлы автоматически фильтруются и сортируются по имени.

## Выходные параметры

| Имя выходного параметра | Описание | Тип данных |
| --- | --- | --- |
| `GEMINI_INPUT_FILES` | Отформатированные данные файла, готовые к использованию с узлами Gemini LLM, содержащие загруженное содержимое файла в соответствующем формате API. | GEMINI_INPUT_FILES |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/GeminiInputFiles/ru.md)

---
**Source fingerprint (SHA-256):** `54da8696d144513efa9660fbc5ddbf5480da12eafe4d2791c8e81cd207ef8a52`
