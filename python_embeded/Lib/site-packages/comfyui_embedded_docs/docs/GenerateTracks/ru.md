# GenerateTracks

Вот перевод документации на русский язык:

Узел `GenerateTracks` создает несколько параллельных траекторий движения для генерации видео. Он определяет основной путь от начальной точки до конечной точки, затем генерирует набор дорожек, которые проходят параллельно этому пути, равномерно распределяясь по ширине. Вы можете управлять формой пути (прямая линия или кривая Безье), скоростью движения вдоль него и кадрами, в которых дорожки видны.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `width` | Ширина видеокадра в пикселях. Значение по умолчанию: 832. | INT | Да | 16 - 4096 |
| `height` | Высота видеокадра в пикселях. Значение по умолчанию: 480. | INT | Да | 16 - 4096 |
| `start_x` | Нормализованная координата X (0-1) для начальной позиции. Значение по умолчанию: 0.0. | FLOAT | Да | 0.0 - 1.0 |
| `start_y` | Нормализованная координата Y (0-1) для начальной позиции. Значение по умолчанию: 0.0. | FLOAT | Да | 0.0 - 1.0 |
| `end_x` | Нормализованная координата X (0-1) для конечной позиции. Значение по умолчанию: 1.0. | FLOAT | Да | 0.0 - 1.0 |
| `end_y` | Нормализованная координата Y (0-1) для конечной позиции. Значение по умолчанию: 1.0. | FLOAT | Да | 0.0 - 1.0 |
| `num_frames` | Общее количество кадров, для которых генерируются позиции дорожек. Значение по умолчанию: 81. | INT | Да | 1 - 1024 |
| `num_tracks` | Количество параллельных дорожек для генерации. Значение по умолчанию: 5. | INT | Да | 1 - 100 |
| `track_spread` | Нормализованное расстояние между дорожками. Дорожки распределяются перпендикулярно направлению движения. Значение по умолчанию: 0.025. | FLOAT | Да | 0.0 - 1.0 |
| `bezier` | Включить путь по кривой Безье, используя среднюю точку в качестве контрольной точки. Значение по умолчанию: False. | BOOLEAN | Да | True / False |
| `mid_x` | Нормализованная контрольная точка X для кривой Безье. Используется только когда параметр 'bezier' включен. Значение по умолчанию: 0.5. | FLOAT | Да | 0.0 - 1.0 |
| `mid_y` | Нормализованная контрольная точка Y для кривой Безье. Используется только когда параметр 'bezier' включен. Значение по умолчанию: 0.5. | FLOAT | Да | 0.0 - 1.0 |
| `interpolation` | Управляет временем/скоростью движения вдоль пути. Значение по умолчанию: "linear". | COMBO | Да | `"linear"`<br>`"ease_in"`<br>`"ease_out"`<br>`"ease_in_out"`<br>`"constant"` |
| `track_mask` | Необязательная маска для указания видимых кадров. | MASK | Нет | - |

**Примечание:** Параметры `mid_x` и `mid_y` используются только когда параметр `bezier` установлен в `True`. Когда `bezier` равен `False`, путь представляет собой прямую линию от начальной до конечной точки.

## Выходные данные

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `TRACKS` | Объект дорожек, содержащий сгенерированные координаты пути и информацию о видимости для всех дорожек во всех кадрах. | TRACKS |
| `track_length` | Количество кадров, для которых были сгенерированы дорожки, соответствует входному параметру `num_frames`. | INT |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/GenerateTracks/ru.md)

---
**Source fingerprint (SHA-256):** `3dca1cabaee8738e2a68acafed47ad347019d03c9b7f0d1392b3fdf97d0e8add`
