# تدفق عينات النموذج المتقطع

تم تصميم هذه العقدة لتعديل سلوك أخذ العينات لنموذج ما من خلال تطبيق استراتيجية أخذ عينات متقطعة. تتيح هذه العقدة اختيار طرق أخذ عينات مختلفة، مثل إبسيلون (epsilon)، أو التنبؤ بالسرعة (v_prediction)، أو إل سي إم (lcm)، أو إكس زيرو (x0)، مع إمكانية ضبط استراتيجية تقليل الضوضاء في النموذج بناءً على إعداد نسبة الضوضاء الصفرية (zsnr).

## المدخلات

| المعامل | الوصف | نوع البيانات | نوع بايثون (Python dtype) |
| --- | --- | --- | --- |
| `النموذج` | النموذج الذي سيتم تطبيق استراتيجية أخذ العينات المتقطعة عليه. هذا المعامل أساسي لأنه يحدد النموذج الأساسي الذي سيخضع للتعديل. | MODEL | `torch.nn.Module` |
| `العينة` | يحدد طريقة أخذ العينات المتقطعة التي سيتم تطبيقها على النموذج. يؤثر اختيار الطريقة على كيفية توليد النموذج للعينات، مما يوفر استراتيجيات مختلفة لأخذ العينات. | COMBO[STRING] | `str` |
| `zsnr` | علامة منطقية، عند تفعيلها، تقوم بضبط استراتيجية تقليل الضوضاء في النموذج بناءً على نسبة الضوضاء الصفرية. يمكن أن يؤثر ذلك على جودة وخصائص العينات المُولَّدة. | `BOOLEAN` | `bool` |

## المخرجات

| المعامل | الوصف | نوع البيانات | نوع بايثون (Python dtype) |
| --- | --- | --- | --- |
| `النموذج` | النموذج المُعدَّل مع تطبيق استراتيجية أخذ العينات المتقطعة. أصبح هذا النموذج الآن قادرًا على توليد عينات باستخدام الطريقة والتعديلات المحددة. | MODEL | `torch.nn.Module` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ModelSamplingDiscrete/ar.md)
