# WanAnimateToVideo

بالتأكيد، إليك الترجمة العربية للوثيقة التقنية لعقدة WanAnimateToVideo مع الالتزام التام بقواعد الترجمة المحددة.

---

عقدة WanAnimateToVideo تقوم بإنشاء محتوى فيديو من خلال دمج مدخلات تكييف متعددة تشمل مراجع الوضعية، وتعابير الوجه، وعناصر الخلفية. تقوم بمعالجة مدخلات فيديو متنوعة لإنشاء تسلسلات متحركة متماسكة مع الحفاظ على الاستمرارية الزمنية عبر الإطارات. تتعامل العقدة مع عمليات الفضاء الكامن ويمكنها تمديد مقاطع الفيديو الموجودة من خلال استمرار أنماط الحركة.

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
| --- | --- | --- | --- | --- |
| `إيجابي` | تكييف إيجابي لتوجيه عملية التوليد نحو المحتوى المطلوب | CONDITIONING | نعم | - |
| `سلبي` | تكييف سلبي لتوجيه عملية التوليد بعيدًا عن المحتوى غير المرغوب فيه | CONDITIONING | نعم | - |
| `vae` | نموذج VAE المستخدم لتشفير وفك تشفير بيانات الصورة | VAE | نعم | - |
| `العرض` | عرض فيديو الإخراج بالبكسل (الافتراضي: 832، الخطوة: 16) | INT | نعم | 16 إلى MAX_RESOLUTION |
| `الارتفاع` | ارتفاع فيديو الإخراج بالبكسل (الافتراضي: 480، الخطوة: 16) | INT | نعم | 16 إلى MAX_RESOLUTION |
| `الطول` | عدد الإطارات المراد توليدها (الافتراضي: 77، الخطوة: 4) | INT | نعم | 1 إلى MAX_RESOLUTION |
| `حجم_الدفعة` | عدد مقاطع الفيديو المراد توليدها في وقت واحد (الافتراضي: 1) | INT | نعم | 1 إلى 4096 |
| `مخرج_رؤية_المقطع` | مخرجات نموذج رؤية CLIP الاختيارية لتكييف إضافي | CLIP_VISION_OUTPUT | لا | - |
| `صورة_مرجعية` | صورة مرجعية تُستخدم كنقطة بداية للتوليد | IMAGE | لا | - |
| `فيديو_الوجه` | مدخل فيديو يوفر توجيهًا لتعابير الوجه | IMAGE | لا | - |
| `فيديو_الوضعية` | مدخل فيديو يوفر توجيهًا للوضعية والحركة | IMAGE | لا | - |
| `الحد_الأقصى_لإطارات_الحركة_المستمرة` | الحد الأقصى لعدد الإطارات المراد الاستمرار منها من الحركة السابقة (الافتراضي: 5، الخطوة: 4) | INT | نعم | 1 إلى MAX_RESOLUTION |
| `فيديو_الخلفية` | فيديو خلفية لدمجه مع المحتوى المُنشأ | IMAGE | لا | - |
| `قناع_الشخصية` | قناع يحدد مناطق الشخصية للمعالجة الانتقائية | MASK | لا | - |
| `مواصلة_الحركة` | تسلسل الحركة السابق للاستمرار منه لتحقيق الاستمرارية الزمنية | IMAGE | لا | - |
| `إزاحة_إطار_الفيديو` | مقدار الإطارات المراد تخطيها في جميع فيديوهات الإدخال. يُستخدم لتوليد مقاطع فيديو أطول عن طريق التقسيم. قم بتوصيله بمخرج video_frame_offset من العقدة السابقة لتمديد الفيديو. (الافتراضي: 0، الخطوة: 1) | INT | نعم | 0 إلى MAX_RESOLUTION |

**قيود المعاملات:**

- عند توفير `pose_video`، سيتم تعديل طول الإخراج ليتناسب مع مدة فيديو الوضعية إذا كان منطق `trim_to_pose_video` نشطًا (مضبوط حاليًا على `False` في الكود المصدري)
- يتم تغيير حجم `face_video` تلقائيًا إلى دقة 512×512 بكسل وتطبيعه إلى نطاق من -1.0 إلى 1.0 عند معالجته
- إطارات `continue_motion` محدودة بمعامل `continue_motion_max_frames`؛ يتم استخدام آخر `continue_motion_max_frames` إطارًا فقط من الإدخال
- يتم إزاحة فيديوهات الإدخال (`face_video`, `pose_video`, `background_video`, `character_mask`) بمقدار `video_frame_offset` قبل المعالجة؛ إذا تجاوز الإزاحة طول الفيديو، يتم تجاهل الإدخال
- إذا كان `character_mask` يحتوي على إطار واحد فقط، فسيتم تكراره عبر جميع الإطارات
- عند توفير `clip_vision_output`، يتم تطبيقه على كل من التكييف الإيجابي والسلبي
- إذا لم يتم توفير `reference_image`، يتم استخدام صورة سوداء (جميع القيم صفر) كمرجع افتراضي
- إذا لم يتم توفير `continue_motion`، يتم ملء الإطارات الأولية بضوضاء رمادية (كثافة 0.5)

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `سلبي` | تكييف إيجابي معدّل مع سياق فيديو إضافي يتضمن مخرجات رؤية CLIP، وكامن فيديو الوضعية، وبكسلات فيديو الوجه، وصورة كامنة متسلسلة، وقناع متسلسل | CONDITIONING |
| `كامن` | تكييف سلبي معدّل مع سياق فيديو إضافي يتضمن مخرجات رؤية CLIP، وكامن فيديو الوضعية، وبكسلات فيديو الوجه (مقلوبة)، وصورة كامنة متسلسلة، وقناع متسلسل | CONDITIONING |
| `قص_الكامن` | محتوى الفيديو المُنشأ بتنسيق الفضاء الكامن ذو الشكل [batch_size, 16, latent_length + trim_latent, latent_height, latent_width] | LATENT |
| `قص_الصورة` | معلومات تقليم الفضاء الكامن تشير إلى عدد الإطارات الكامنة المراد قصها من البداية (تتوافق مع الإطارات الكامنة للصورة المرجعية) | INT |
| `إزاحة_إطار_الفيديو` | معلومات تقليم مساحة الصورة لإطارات الحركة المرجعية، تشير إلى عدد إطارات الصورة المراد قصها من البداية | INT |
| `إزاحة_إطار_الفيديو` | إزاحة الإطارات المحدثة لمواصلة توليد الفيديو على شكل أجزاء، محسوبة على أنها الإزاحة السابقة بالإضافة إلى الطول المُنشأ | INT |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/WanAnimateToVideo/ar.md)

---
**Source fingerprint (SHA-256):** `c2ca90f4963f629d51cdd7f4bdb67e01c32ce5ca7d916b1f992ccd220f57566c`
