# نمونه‌بردار ویدئوی AR

گره Sampler AR Video یک روش نمونه‌گیری تخصصی برای مدل‌های ویدئویی خودرگرسیو (Autoregressive) ارائه می‌دهد، مانند مدل‌هایی که از تکنیک‌های Causal Forcing یا Self-Forcing استفاده می‌کنند. این گره تمام پارامترهای مربوط به حلقه خودرگرسیو (AR) را مستقیماً درون گردش کار مدیریت می‌کند و پیکربندی نحوه تولید فریم‌های ویدئو توسط مدل را به صورت گام‌به‌گام آسان می‌سازد.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | ضروری | محدوده |
| --- | --- | --- | --- | --- |
| `تعداد فریم در هر بلوک` | تعداد فریم‌ها در هر بلوک خودرگرسیو. مقدار ۱ به این معناست که مدل هر بار یک فریم (فریم‌به‌فریم) تولید می‌کند، در حالی که مقدار ۳ به معنای تولید سه فریم با هم (تکه‌ای) است. این تنظیم باید با حالت آموزش checkpoint مطابقت داشته باشد. پیش‌فرض: ۱. | INT | بله | ۱ تا ۶۴ |

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
| --- | --- | --- |
| `SAMPLER` | یک شیء نمونه‌گیر پیکربندی‌شده که از تابع نمونه‌گیری "ar_video" با پارامترهای خودرگرسیو مشخص‌شده استفاده می‌کند. | SAMPLER |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/SamplerARVideo/fa.md)

---
**Source fingerprint (SHA-256):** `5b735f98fdde074ee9483503fee0e2322d510aed846336b382a8ea89a363c9e4`
