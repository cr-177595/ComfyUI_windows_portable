# Вычитание CLIP

Узел CLIPMergeSubtract выполняет слияние моделей путем вычитания весов одной модели CLIP из другой. Он создает новую модель CLIP, клонируя первую модель, а затем вычитая ключевые патчи из второй модели с регулируемым множителем для управления силой вычитания. Это позволяет выполнять тонкую настройку смешивания моделей путем удаления определенных характеристик из базовой модели.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `clip1` | Базовая модель CLIP, которая будет клонирована и изменена | CLIP | Да | - |
| `clip2` | Модель CLIP, ключевые патчи которой будут вычтены из базовой модели | CLIP | Да | - |
| `множитель` | Управляет силой операции вычитания (по умолчанию: 1.0) | FLOAT | Да | от -10.0 до 10.0 |

**Примечание:** Узел исключает параметры `.position_ids` и `.logit_scale` из операции вычитания независимо от значения множителя.

## Выходные параметры

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `clip` | Результирующая модель CLIP после вычитания весов второй модели из первой | CLIP |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/CLIPMergeSubtract/ru.md)

---
**Source fingerprint (SHA-256):** `3136cf509fcbfa291af8f820928a6cc14de7a586f953af0ada9bea949b437d86`
