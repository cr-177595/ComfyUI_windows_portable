# DCTestNode

عقدة DCTestNode هي عقدة منطقية تُرجع أنواعًا مختلفة من البيانات بناءً على اختيار المستخدم من قائمة منسدلة ديناميكية. تعمل كموجه شرطي، حيث يحدد الخيار المختار أي حقل إدخال نشط ونوع القيمة التي ستُخرجها العقدة.

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
| --- | --- | --- | --- | --- |
| `combo` | الاختيار الرئيسي الذي يحدد أي حقل إدخال نشط وما ستُخرجه العقدة. | COMBO | نعم | `"option1"`<br>`"option2"`<br>`"option3"`<br>`"option4"` |
| `string` | حقل إدخال نصي. هذا الحقل نشط وإلزامي فقط عندما تكون قيمة `combo` مضبوطة على `"option1"`. | STRING | لا | - |
| `integer` | حقل إدخال عدد صحيح. هذا الحقل نشط وإلزامي فقط عندما تكون قيمة `combo` مضبوطة على `"option2"`. | INT | لا | - |
| `image` | حقل إدخال صورة. هذا الحقل نشط وإلزامي فقط عندما تكون قيمة `combo` مضبوطة على `"option3"`. | IMAGE | لا | - |
| `subcombo` | اختيار ثانوي يظهر عندما تكون قيمة `combo` مضبوطة على `"option4"`. يحدد أي حقول الإدخال المتداخلة نشطة. | COMBO | لا | `"opt1"`<br>`"opt2"` |
| `float_x` | إدخال رقم عشري. هذا الحقل نشط وإلزامي فقط عندما تكون قيمة `combo` مضبوطة على `"option4"` وقيمة `subcombo` مضبوطة على `"opt1"`. | FLOAT | لا | - |
| `float_y` | إدخال رقم عشري. هذا الحقل نشط وإلزامي فقط عندما تكون قيمة `combo` مضبوطة على `"option4"` وقيمة `subcombo` مضبوطة على `"opt1"`. | FLOAT | لا | - |
| `mask1` | حقل إدخال قناع. هذا الحقل نشط فقط عندما تكون قيمة `combo` مضبوطة على `"option4"` وقيمة `subcombo` مضبوطة على `"opt2"`. وهو اختياري. | MASK | لا | - |

**قيود المعاملات:**

* يتحكم معامل `combo` في ظهور وإلزامية جميع حقول الإدخال الأخرى. سيتم عرض المدخلات المرتبطة بخيار `combo` المحدد فقط وتكون إلزامية (باستثناء `mask1` وهو اختياري).
* عندما تكون قيمة `combo` مضبوطة على `"option4"`، يصبح معامل `subcombo` إلزاميًا ويتحكم في مجموعة ثانية من المدخلات المتداخلة (`float_x`/`float_y` أو `mask1`).

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `output` | يعتمد المخرج على خيار `combo` المحدد. يمكن أن يكون STRING (`"option1"`)، أو INT (`"option2"`)، أو IMAGE (`"option3"`)، أو تمثيل نصي لقاموس `subcombo` (`"option4"`). | ANYTYPE |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/DCTestNode/ar.md)

---
**Source fingerprint (SHA-256):** `98c4ca2100a27594df360935cc1507960480fe75a76ca0df2af75925d399be00`
