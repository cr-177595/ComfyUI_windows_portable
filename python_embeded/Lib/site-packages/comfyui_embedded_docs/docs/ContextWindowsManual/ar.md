# Context Windows (Manual)

بالتأكيد، إليك الترجمة العربية للوثيقة التقنية الخاصة بعقدة "Context Windows (Manual)" في ComfyUI، مع الالتزام التام بقواعد الترجمة المحددة:

تتيح لك عقدة "النوافذ السياقية (يدوي)" (Context Windows (Manual)) تكوين النوافذ السياقية يدويًا للنماذج أثناء عملية أخذ العينات (sampling). تقوم هذه العقدة بإنشاء مقاطع سياقية متداخلة بطول وتداخل وأنماط جدولة محددة لمعالجة البيانات في أجزاء يمكن التحكم فيها مع الحفاظ على الاستمرارية بين المقاطع. توفر هذه العقدة خيارات متقدمة للتحكم في كيفية تطبيق النوافذ السياقية، بما في ذلك خلط الضوضاء (noise shuffling)، والاحتفاظ بالشروط (conditioning retention)، وإصلاح النوافذ السببية (causal window fixes).

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
| --- | --- | --- | --- | --- |
| `model` | النموذج الذي سيتم تطبيق النوافذ السياقية عليه أثناء أخذ العينات. | MODEL | نعم | - |
| `context_length` | طول النافذة السياقية (الافتراضي: 16). | INT | لا | 1+ |
| `context_overlap` | مقدار التداخل في النافذة السياقية (الافتراضي: 4). | INT | لا | 0+ |
| `context_schedule` | خطوة (stride) النافذة السياقية. | COMBO | لا | `STATIC_STANDARD`<br>`UNIFORM_STANDARD`<br>`UNIFORM_LOOPED`<br>`BATCHED` |
| `context_stride` | خطوة النافذة السياقية؛ تنطبق فقط على الجداول المنتظمة (uniform schedules) (الافتراضي: 1). | INT | لا | 1+ |
| `closed_loop` | ما إذا كان سيتم إغلاق حلقة النافذة السياقية؛ تنطبق فقط على الجداول الحلقية (looped schedules) (الافتراضي: False). | BOOLEAN | لا | - |
| `fuse_method` | الطريقة المستخدمة لدمج النوافذ السياقية (الافتراضي: PYRAMID). | COMBO | لا | `PYRAMID`<br>`LIST_STATIC` |
| `dim` | البُعد الذي سيتم تطبيق النوافذ السياقية عليه (الافتراضي: 0). | INT | لا | 0-5 |
| `ضجيج حر` | ما إذا كان سيتم تطبيق خلط ضوضاء FreeNoise، مما يحسن مزج النوافذ (الافتراضي: False). | BOOLEAN | لا | - |
| `cond_retain_index_list` | قائمة بمؤشرات (indices) المساحة الكامنة (latent) التي سيتم الاحتفاظ بها في موترات الشروط (conditioning tensors) لكل نافذة. على سبيل المثال، تعيين هذا إلى '0' سيستخدم الصورة الأولية لكل نافذة (الافتراضي: ""). | STRING | لا | - |
| `split_conds_to_windows` | ما إذا كان سيتم تقسيم الشروط المتعددة (المنشأة بواسطة ConditionCombine) إلى كل نافذة بناءً على مؤشر المنطقة (region index) (الافتراضي: False). | BOOLEAN | لا | - |
| `causal_window_fix` | ما إذا كان سيتم إضافة إطار إصلاح سببي (causal fix frame) إلى النوافذ السياقية غير المفهرسة بالرقم 0 (الافتراضي: True). | BOOLEAN | لا | - |

**قيود المعاملات:**

- يتم استخدام `context_stride` فقط عند تحديد جداول منتظمة (uniform schedules)
- `closed_loop` ينطبق فقط على الجداول الحلقية (looped schedules)
- يجب أن تكون قيمة `dim` بين 0 و 5 شاملة
- يتوقع `cond_retain_index_list` قائمة مفصولة بفواصل من المؤشرات الصحيحة كسلسلة نصية (على سبيل المثال، "0,1,2")

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `model` | النموذج مع تطبيق النوافذ السياقية أثناء أخذ العينات. | MODEL |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ContextWindowsManual/ar.md)

---
**Source fingerprint (SHA-256):** `b05ddda0ba38588305e6f733cd218c8b462268c39d16226ca961d09054187261`
