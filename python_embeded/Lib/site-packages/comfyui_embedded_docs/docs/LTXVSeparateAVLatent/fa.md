# LTXVSeparateAVLatent

### LTXVSeparateAVLatent

گره LTXVSeparateAVLatent یک نمایش نهفته ترکیبی صوتی-تصویری را دریافت کرده و آن را به دو بخش مجزا تقسیم می‌کند: یکی برای ویدئو و دیگری برای صدا. این گره نمونه‌ها و در صورت وجود، ماسک‌های نویز را از ورودی نهفته جدا کرده و دو شیء نهفته جدید ایجاد می‌کند.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | ضروری | محدوده |
| --- | --- | --- | --- | --- |
| `av_latent` | نمایش نهفته ترکیبی صوتی-تصویری که قرار است جدا شود. | LATENT | بله | نامشخص |

**نکته:** تنسور `samples` ورودی نهفته باید حداقل دو عنصر در امتداد بعد اول (بعد دسته‌ای) داشته باشد. اولین عنصر برای نهفته ویدئو و دومین عنصر برای نهفته صدا استفاده می‌شود. اگر `noise_mask` وجود داشته باشد، به همین ترتیب تقسیم می‌شود.

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
| --- | --- | --- |
| `video_latent` | نمایش نهفته حاوی داده‌های ویدئوی جدا شده. | LATENT |
| `audio_latent` | نمایش نهفته حاوی داده‌های صدای جدا شده. | LATENT |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LTXVSeparateAVLatent/fa.md)

---
**Source fingerprint (SHA-256):** `55bce5d768e7fe13f885cc32d34ecdac5cdcbb667b03743004866ea4b6d58d46`
