# ماسک چهره MediaPipe

## نمای کلی

این گره یک ماسک باینری (تصویر سیاه و سفید) بر اساس نقاط کلیدی صورت شناسایی‌شده توسط MediaPipe ایجاد می‌کند. اشکال چندضلعی توپر را برای هر ناحیه صورت شناسایی‌شده رسم کرده و به ازای هر فریم در یک بچ، یک ماسک تولید می‌کند. هنگامی که چندین صورت در یک فریم شناسایی شوند، ماسک‌های آن‌ها در یک ماسک واحد ترکیب می‌شوند.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | ضروری | محدوده |
| --- | --- | --- | --- | --- |
| `face_landmarks` | داده‌های نقاط کلیدی صورت از یک گره تشخیص صورت MediaPipe. | FACE_LANDMARKS | بله | - |
| `regions` | نواحی صورت را که در ماسک گنجانده می‌شوند انتخاب می‌کند. `"all"` ماسکی از اجتماع تمام نواحی صورت (بیضی صورت، لب‌ها، چشم‌ها، عنبیه‌ها) ایجاد می‌کند. `"custom"` به شما امکان می‌دهد هر ناحیه را به‌صورت جداگانه فعال/غیرفعال کنید. پیش‌فرض: `"all"` | COMBO | بله | `"all"`<br>`"custom"` |

هنگامی که `regions` روی `"custom"` تنظیم شود، پارامترهای بولی اضافی زیر در دسترس قرار می‌گیرند:

| پارامتر | توضیحات | نوع داده | ضروری | محدوده |
| --- | --- | --- | --- | --- |
| `face_oval` | ناحیه بیضی صورت را در ماسک شامل شود. پیش‌فرض: True | BOOLEAN | خیر | True/False |
| `lips` | ناحیه لب‌ها را در ماسک شامل شود. پیش‌فرض: True | BOOLEAN | خیر | True/False |
| `eyes` | ناحیه چشم‌ها را در ماسک شامل شود. پیش‌فرض: True | BOOLEAN | خیر | True/False |
| `irises` | ناحیه عنبیه‌ها را در ماسک شامل شود. پیش‌فرض: True | BOOLEAN | خیر | True/False |

**نکته:** هنگام استفاده از حالت `"all"`، ماسک شامل تمام نواحی ترکیب‌شده است. از آنجایی که بیضی صورت سایر نواحی را در بر می‌گیرد، انتخاب `"all"` عملاً همان نتیجه انتخاب تنها بیضی صورت را تولید می‌کند.

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
| --- | --- | --- |
| `MASK` | یک تنسور ماسک باینری که در آن نواحی صورت سفید (مقدار 1.0) و پس‌زمینه سیاه (مقدار 0.0) است. ماسک ابعادی مشابه تصویر ورودی دارد و به ازای هر فریم در بچ، یک ماسک شامل می‌شود. | MASK |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/MediaPipeFaceMask/fa.md)

---
**Source fingerprint (SHA-256):** `92270002a42ed59bc75e676a6881e1899186d3c8a1bb4dd4c0d39b3762b5bb66`
