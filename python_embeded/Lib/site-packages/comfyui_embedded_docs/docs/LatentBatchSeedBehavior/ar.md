# سلوك بذرة دفعة الكامن

عقدة سلوك البذرة للدفعة الكامنة (LatentBatchSeedBehavior) مصممة لتعديل سلوك البذرة (seed) لمجموعة من العينات الكامنة. تتيح هذه العقدة إما عشوائية البذرة أو تثبيتها عبر الدفعة، مما يؤثر على عملية التوليد إما بإدخال التباين أو الحفاظ على الاتساق في المخرجات المُولَّدة.

## المدخلات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `samples` | يمثل معامل "العينات" الدفعة من العينات الكامنة المراد معالجتها. يعتمد تعديله على سلوك البذرة المُختار، مما يؤثر على اتساق المخرجات المُولَّدة أو تباينها. | `LATENT` |
| `seed_behavior` | يحدد معامل "سلوك البذرة" ما إذا كانت بذرة الدفعة من العينات الكامنة يجب أن تكون عشوائية أم ثابتة. يؤثر هذا الاختيار بشكل كبير على عملية التوليد إما بإدخال التباين أو ضمان الاتساق عبر الدفعة. | COMBO[STRING] |

## المخرجات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `latent` | المخرج هو نسخة معدلة من العينات الكامنة المُدخلة، مع تعديلات تم إجراؤها بناءً على سلوك البذرة المُحدد. إما أن يحافظ على فهرس الدفعة أو يغيره ليعكس سلوك البذرة المُختار. | `LATENT` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LatentBatchSeedBehavior/ar.md)
