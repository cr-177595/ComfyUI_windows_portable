# كليينج إطار البداية-النهاية إلى فيديو

بالتأكيد، إليك الترجمة العربية للوثيقة التقنية الخاصة بعقدة ComfyUI، مع الالتزام التام بقواعد الترجمة المحددة:

## عقدة تحويل الإطار البداية-النهاية إلى فيديو (Kling)

تنشئ عقدة "إطار البداية-النهاية إلى فيديو" (Kling) تسلسلاً فيديوياً ينتقل بسلاسة بين صورتي البداية والنهاية التي تقدمها. تقوم هذه العقدة بتوليد جميع الإطارات الوسيطة لإنتاج تحول سلس من الإطار الأول إلى الإطار الأخير. تستدعي هذه العقدة واجهة برمجة التطبيقات (API) لتحويل الصورة إلى فيديو، ولكنها تدعم فقط خيارات الإدخال المتوافقة مع حقل الطلب `image_tail`.

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
| --- | --- | --- | --- | --- |
| `start_frame` | الصورة المرجعية - رابط URL أو سلسلة مشفرة بـ Base64، يجب ألا يتجاوز حجمها 10 ميجابايت، ودقة لا تقل عن 300*300 بكسل، ونسبة عرض إلى ارتفاع بين 1:2.5 ~ 2.5:1. يجب ألا يتضمن ترميز Base64 البادئة data:image. | IMAGE | نعم | - |
| `end_frame` | الصورة المرجعية - التحكم في الإطار النهائي. رابط URL أو سلسلة مشفرة بـ Base64، يجب ألا يتجاوز حجمها 10 ميجابايت، ودقة لا تقل عن 300*300 بكسل. يجب ألا يتضمن ترميز Base64 البادئة data:image. | IMAGE | نعم | - |
| `prompt` | موجه النص الإيجابي | STRING | نعم | - |
| `negative_prompt` | موجه النص السلبي | STRING | نعم | - |
| `cfg_scale` | يتحكم في قوة توجيه الموجه (القيمة الافتراضية: 0.5) | FLOAT | لا | 0.0-1.0 |
| `aspect_ratio` | نسبة العرض إلى الارتفاع للفيديو المُنشأ (القيمة الافتراضية: "16:9") | COMBO | لا | "16:9"<br>"9:16"<br>"1:1" |
| `mode` | التكوين المستخدم لتوليد الفيديو باتباع التنسيق: الوضع / المدة / اسم_النموذج. (القيمة الافتراضية: الخيار السابع من الأوضاع المتاحة) | COMBO | لا | تتوفر خيارات متعددة |

**قيود الصورة:**

- يجب تقديم كل من `start_frame` و `end_frame`، ويجب ألا يتجاوز حجم كل منهما 10 ميجابايت
- الحد الأدنى للدقة: 300×300 بكسل لكلتا الصورتين
- يجب أن تكون نسبة العرض إلى الارتفاع لـ `start_frame` بين 1:2.5 و 2.5:1
- يجب ألا تتضمن الصور المشفرة بـ Base64 البادئة "data:image"

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `video_id` | تسلسل الفيديو المُنشأ | VIDEO |
| `duration` | معرف فريد للفيديو المُنشأ | STRING |
| `duration` | مدة الفيديو المُنشأ | STRING |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/KlingStartEndFrameNode/ar.md)

---
**Source fingerprint (SHA-256):** `1df5820b4f41ccd5afec8e2701888d90c940f164c433c7f81397b41e8fc333c6`
