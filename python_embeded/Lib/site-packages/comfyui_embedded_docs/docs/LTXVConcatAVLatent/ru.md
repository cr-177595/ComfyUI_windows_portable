# LTXVConcatAVLatent

Узел LTXVConcatAVLatent объединяет латентное представление видео и латентное представление аудио в единый конкатенированный латентный выходной сигнал. Он объединяет тензоры `samples` из обоих входов, а также, при наличии, их тензоры `noise_mask`, подготавливая их для дальнейшей обработки в конвейере генерации видео.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `video_latent` | Латентное представление видеоданных. | LATENT | Да |  |
| `audio_latent` | Латентное представление аудиоданных. | LATENT | Да |  |

**Примечание:** Тензоры `samples` из входных данных `video_latent` и `audio_latent` конкатенируются. Если какой-либо из входов содержит `noise_mask`, он будет использован; если один из них отсутствует, для него создается маска из единиц (той же формы, что и соответствующий `samples`). Затем полученные маски также конкатенируются.

## Выходные параметры

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `latent` | Единый латентный словарь, содержащий конкатенированные `samples` и, при необходимости, конкатенированный `noise_mask` из видео- и аудиовходов. | LATENT |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LTXVConcatAVLatent/ru.md)

---
**Source fingerprint (SHA-256):** `322d6870f110fb1ef8b472cb49649cc9fff7865f4c7a83fbfd536f1fdfd694f8`
