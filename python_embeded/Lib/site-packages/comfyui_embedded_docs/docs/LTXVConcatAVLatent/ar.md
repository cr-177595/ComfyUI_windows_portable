# LTXVConcatAVLatent

هذه ترجمة الوثيقة التقنية لعقدة **LTXVConcatAVLatent** الخاصة بـ ComfyUI:

## نظرة عامة

تقوم عقدة LTXVConcatAVLatent بدمج التمثيل الكامن للفيديو والتمثيل الكامن للصوت في مخرج كامن واحد مترابط. تدمج هذه العقدة موترات `samples` من كلا المدخلين، بالإضافة إلى موترات `noise_mask` الخاصة بهما إن وجدت، لتحضيرها لمزيد من المعالجة في خط أنابيب توليد الفيديو.

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
| --- | --- | --- | --- | --- |
| `video_latent` | التمثيل الكامن لبيانات الفيديو. | LATENT | نعم |  |
| `audio_latent` | التمثيل الكامن لبيانات الصوت. | LATENT | نعم |  |

**ملاحظة:** يتم دمج موترات `samples` من مدخلي `video_latent` و `audio_latent`. إذا احتوى أي من المدخلين على `noise_mask`، فسيتم استخدامه؛ أما إذا كان أحد المدخلين يفتقر إليه، فسيتم إنشاء قناع من الآحاد (بنفس شكل موتر `samples` المقابل له). ثم يتم دمج القناعين الناتجين أيضًا.

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `latent` | قاموس كامن واحد يحتوي على موتر `samples` المدمج، وإذا أمكن، موتر `noise_mask` المدمج من مدخلي الفيديو والصوت. | LATENT |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LTXVConcatAVLatent/ar.md)

---
**Source fingerprint (SHA-256):** `322d6870f110fb1ef8b472cb49649cc9fff7865f4c7a83fbfd536f1fdfd694f8`
