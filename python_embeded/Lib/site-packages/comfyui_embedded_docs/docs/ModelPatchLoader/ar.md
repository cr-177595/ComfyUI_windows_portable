# ModelPatchLoader

هذه ترجمة الوثيقة التقنية لعقدة **ModelPatchLoader** في ComfyUI إلى العربية، مع الالتزام بقواعد الترجمة المحددة:

---

عقدة **ModelPatchLoader** تقوم بتحميل تصحيحات النماذج المتخصصة من مجلد `model_patches`. تكتشف العقدة تلقائيًا نوع ملف التصحيح وتحمّل بنية النموذج المناسبة، ثم تغلفها في كائن `ModelPatcher` لاستخدامها في سير العمل. تدعم هذه العقدة أنواعًا مختلفة من التصحيحات، بما في ذلك كتل التحكم (controlnet blocks)، ونماذج تضمين الميزات (feature embedder models)، وغيرها من البنى المتخصصة.

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
| --- | --- | --- | --- | --- |
| `الاسم` | اسم ملف التصحيح المراد تحميله من دليل model_patches | STRING | نعم | جميع ملفات تصحيح النماذج المتاحة من مجلد model_patches |

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `MODEL_PATCH` | تصحيح النموذج المُحمّل والمُغلف في كائن ModelPatcher لاستخدامه في سير العمل | MODEL_PATCH |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ModelPatchLoader/ar.md)

---
**Source fingerprint (SHA-256):** `e394e165cf416019ed53d9fde42d97c3c9b9f9afd843b12371a624467a4841bf`
