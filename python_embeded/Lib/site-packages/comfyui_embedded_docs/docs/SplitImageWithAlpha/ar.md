# فصل الصورة مع ألفا

عقدة SplitImageWithAlpha مصممة لفصل مكونات اللون وقناة الشفافية (ألفا) للصورة. تقوم بمعالجة موتر الصورة المدخلة، واستخراج قنوات RGB كمكون اللون وقناة ألفا كمكون الشفافية، مما يسهل العمليات التي تتطلب معالجة هذه الجوانب المتميزة للصورة.

## المدخلات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `الصورة` | يمثل المعامل 'image' موتر الصورة المدخلة الذي سيتم فصل قنوات RGB وقناة ألفا منه. وهو ضروري للتشغيل حيث يوفر البيانات المصدر لعملية الفصل. | `IMAGE` |

## المخرجات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `الصورة` | يمثل المخرج 'image' قنوات RGB المنفصلة للصورة المدخلة، مما يوفر مكون اللون دون معلومات الشفافية. | `IMAGE` |
| `mask` | يمثل المخرج 'mask' قناة ألفا المنفصلة للصورة المدخلة، مما يوفر معلومات الشفافية. | `MASK` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/SplitImageWithAlpha/ar.md)
