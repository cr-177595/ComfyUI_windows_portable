# Преобразование цвета в RGB Int

Вот перевод документации на русский язык:

Узел ColorToRGBInt преобразует цвет, заданный в шестнадцатеричном формате, в целочисленное значение. Он принимает строку цвета, например `#FF5733`, и вычисляет соответствующее целое число RGB путем объединения красного, зеленого и синего компонентов.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `цвет` | Значение цвета в шестнадцатеричном формате `#RRGGBB`. | STRING | Да | Н/Д |

**Примечание:** Входная строка `color` должна содержать ровно 7 символов и начинаться с символа `#`, за которым следуют шесть шестнадцатеричных цифр (например, `#FF0000` для красного цвета). Узел выдаст ошибку, если формат будет некорректным.

## Выходные параметры

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `rgb_int` | Вычисленное целочисленное значение RGB. Оно получается по формуле: `(Red * 65536) + (Green * 256) + Blue`. | INT |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ColorToRGBInt/ru.md)

---
**Source fingerprint (SHA-256):** `5b8617d6b28caaa5f01dad1c6a302fa321f1bd53a0454451d468e36747e70e8f`
