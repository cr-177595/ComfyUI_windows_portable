# CLIPTextEncodeSDXL

این گره برای رمزگذاری ورودی متنی با استفاده از یک مدل CLIP که به‌طور خاص برای معماری SDXL سفارشی‌سازی شده است، طراحی شده است. این گره از یک سیستم رمزگذار دوگانه (CLIP-L و CLIP-G) برای پردازش توصیف‌های متنی استفاده می‌کند که منجر به تولید تصاویر دقیق‌تری می‌شود.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده |
| --- | --- | --- |
| `clip` | نمونه مدل CLIP مورد استفاده برای رمزگذاری متن. | CLIP |
| `عرض` | عرض تصویر را بر حسب پیکسل مشخص می‌کند، پیش‌فرض 1024. | INT |
| `ارتفاع` | ارتفاع تصویر را بر حسب پیکسل مشخص می‌کند، پیش‌فرض 1024. | INT |
| `برش_عرض` | عرض ناحیه برش بر حسب پیکسل، پیش‌فرض 0. | INT |
| `برش_ارتفاع` | ارتفاع ناحیه برش بر حسب پیکسل، پیش‌فرض 0. | INT |
| `عرض هدف` | عرض هدف برای تصویر خروجی، پیش‌فرض 1024. | INT |
| `ارتفاع هدف` | ارتفاع هدف برای تصویر خروجی، پیش‌فرض 1024. | INT |
| `متن_g` | توصیف متنی سراسری برای توصیف کلی صحنه. | STRING |
| `متن_l` | توصیف متنی محلی برای توصیف جزئیات. | STRING |

## خروجی‌ها

| پارامتر | توضیحات | نوع داده |
| --- | --- | --- |
| `CONDITIONING` | شامل متن رمزگذاری‌شده و اطلاعات شرطی مورد نیاز برای تولید تصویر است. | CONDITIONING |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/CLIPTextEncodeSDXL/fa.md)
