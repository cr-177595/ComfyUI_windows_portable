# LTXV صورة إلى فيديو

أنت خبير في الترجمة التقنية متخصص في توثيق عُقد ComfyUI من الإنجليزية إلى العربية.

## قواعد الترجمة

1. **المحتوى الذي يجب عدم ترجمته:**
   - أسماء المعاملات بين علامات الاقتباس الخلفية: `image`, `seed`, `model`
   - أنواع البيانات بالأحرف الكبيرة: IMAGE, STRING, INT, FLOAT, MODEL, CONDITIONING, إلخ
   - القيم في عمود Range: الأرقام، "auto"، أسماء الخيارات
   - الكود، مسارات الملفات

2. **المحتوى الذي يجب ترجمته:**
   - عناوين الأقسام: ## نظرة عامة, ## المدخلات, ## المخرجات
   - جميع النصوص الوصفية والتوضيحية
   - أوصاف المعاملات

3. **جودة الترجمة:**
   - استخدام اللغة العربية الفصحى المعاصرة
   - الحفاظ على نبرة احترافية ولكن سهلة الفهم
   - ضمان الدقة التقنية
   - استخدام المصطلحات التقنية العربية القياسية

4. **التنسيق:**
   - الحفاظ على جميع تنسيقات Markdown
   - الحفاظ على بنية الجداول
   - عدم إضافة أي ملاحظة أو رابط في بداية الوثيقة (سيتم إضافتها تلقائيًا)

الرجاء ترجمة الوثيقة التالية إلى العربية، دون تضمين الملاحظة الأولية للوثيقة:

تقوم عقدة LTXV Image To Video بإنشاء فيديو بجودة احترافية من صورة بداية واحدة. تستخدم العقدة واجهة برمجة تطبيقات خارجية لإنشاء سلسلة فيديو بناءً على النص التوجيهي الخاص بك، مما يسمح لك بتخصيص المدة والدقة ومعدل الإطارات.

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
| --- | --- | --- | --- | --- |
| `صورة` | الإطار الأول الذي سيتم استخدامه للفيديو. | IMAGE | نعم | - |
| `نموذج` | نموذج الذكاء الاصطناعي المستخدم لتوليد الفيديو. نموذج "Fast" محسّن للسرعة، بينما يعطي نموذج "Quality" الأولوية للدقة البصرية. | COMBO | نعم | `"LTX-2 (Fast)"`<br>`"LTX-2 (Quality)"` |
| `مُوجِّه` | وصف نصي يوجه محتوى وحركة الفيديو المُنشأ. | STRING | نعم | - |
| `مدة` | طول الفيديو بالثواني (الافتراضي: 8). | COMBO | نعم | `6`<br>`8`<br>`10`<br>`12`<br>`14`<br>`16`<br>`18`<br>`20` |
| `دقة` | دقة الإخراج للفيديو المُنشأ. | COMBO | نعم | `"1920x1080"`<br>`"2560x1440"`<br>`"3840x2160"` |
| `معدل الإطارات` | عدد الإطارات في الثانية للفيديو (الافتراضي: 25). | COMBO | نعم | `25`<br>`50` |
| `إنشاء_صوت` | عند التفعيل، سيتضمن الفيديو المُنشأ صوتًا مولّدًا بالذكاء الاصطناعي يتوافق مع المشهد (الافتراضي: False). | BOOLEAN | لا | - |

**قيود مهمة:**

* يجب أن يحتوي مدخل `image` على صورة واحدة بالضبط.
* يجب أن يتراوح طول النص التوجيهي `prompt` بين حرف واحد و10,000 حرف.
* إذا اخترت `duration` أطول من 10 ثوانٍ، فيجب عليك استخدام نموذج **"LTX-2 (Fast)"** ودقة **"1920x1080"** ومعدل إطارات **25** FPS. هذه المجموعة إلزامية لمقاطع الفيديو الأطول.

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `video` | ملف الفيديو المُنشأ. | VIDEO |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LtxvApiImageToVideo/ar.md)

---
**Source fingerprint (SHA-256):** `af891b45997173c3210d3de4f7b6bd05b14e9d3bf8a94dcb2c1ce08038b7d99d`
