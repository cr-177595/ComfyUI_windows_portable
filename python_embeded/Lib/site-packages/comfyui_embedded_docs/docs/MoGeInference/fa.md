# استنتاج MoGe

## مرور کلی

مدل MoGe را روی یک تصویر اجرا می‌کند تا عمق و هندسه را تخمین بزند. این گره یک تصویر ورودی را از طریق مدل MoGe پردازش کرده و یک ابرنقطه سه‌بعدی، نقشه عمق، پارامترهای ذاتی دوربین، یک ماسک و نرمال‌های سطح تولید می‌کند.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
| --- | --- | --- | --- | --- |
| `moge_model` | مدل MoGe مورد استفاده برای استنتاج. | MOGE_MODEL | بله | نامشخص |
| `image` | تصویر ورودی برای تخمین عمق و هندسه. | IMAGE | بله | نامشخص |
| `resolution_level` | سطح وضوح پردازش را کنترل می‌کند. 0 سریع‌ترین و 9 بیشترین جزئیات را ارائه می‌دهد. (پیش‌فرض: 9) | INT | بله | 0 تا 9 |
| `fov_x_degrees` | میدان دید افقی دوربین منبع بر حسب درجه. فاصله کانونی مورد استفاده برای تبدیل نقشه عمق به فضای سه‌بعدی را تنظیم می‌کند. مقدار 0.0 به‌طور خودکار میدان دید را از نقاط پیش‌بینی‌شده بازیابی می‌کند. (پیش‌فرض: 0.0) | FLOAT | بله | 0.0 تا 170.0 |
| `batch_size` | تعداد تصاویر پردازش‌شده در هر فراخوانی استنتاج. در صورت اتمام حافظه هنگام پردازش ویدیوهای طولانی یا مجموعه‌های تصویری بزرگ، این مقدار را کاهش دهید. (پیش‌فرض: 4) | INT | بله | 1 تا 64 |
| `force_projection` | (پیشرفته) نقاط پیش‌بینی‌شده را به‌اجبار تصویربرداری می‌کند. (پیش‌فرض: True) | BOOLEAN | بله | True/False |
| `apply_mask` | در صورت فعال بودن، پیکسل‌های ماسک‌شده (آسمان یا نامعتبر) را در خروجی نقاط و عمق به بی‌نهایت تنظیم می‌کند. این کار به ابزارهای مش‌سازی کمک می‌کند تا این نواحی را نادیده بگیرند. برای حفظ هندسه خام پیش‌بینی‌شده در همه جا غیرفعال کنید؛ ماسک همچنان به‌صورت جداگانه بازگردانده می‌شود. (پیش‌فرض: True) | BOOLEAN | بله | True/False |

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
| --- | --- | --- |
| `moge_geometry` | یک دیکشنری شامل هندسه تخمین‌زده‌شده. شامل `image` اصلی بوده و ممکن است شامل `points` (ابرنقطه سه‌بعدی)، `depth` (نقشه عمق)، `intrinsics` (ماتریس پارامترهای ذاتی دوربین)، `mask` (ماسک شناسایی پیکسل‌های معتبر) و `normal` (نرمال‌های سطح) باشد. | MOGE_GEOMETRY |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/MoGeInference/fa.md)

---
**Source fingerprint (SHA-256):** `5213b280513850eeef2e22ae723ebb015789109435e28ddd79f91f9a4b4a1e79`
