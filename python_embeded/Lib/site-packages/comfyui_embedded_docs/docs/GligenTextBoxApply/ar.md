# تطبيق صندوق نص GLIGEN

عقدة `GLIGENTextBoxApply` مصممة لدمج التكييف النصي في مدخلات النموذج التوليدي، وذلك بتطبيق معاملات مربع النص وترميزها باستخدام نموذج CLIP. تعمل هذه العملية على إثراء التكييف بمعلومات مكانية ونصية، مما يسهل عملية التوليد الأكثر دقة ووعياً بالسياق.

## المدخلات

| المعامل | الوصف | نوع Comfy |
| --- | --- | --- |
| `التكييف_إلى` | يحدد مدخل التكييف الأولي الذي ستُضاف إليه معاملات مربع النص والمعلومات النصية المشفرة. يلعب دوراً حاسماً في تحديد المخرجات النهائية من خلال دمج بيانات التكييف الجديدة. | `CONDITIONING` |
| `clip` | نموذج CLIP المستخدم لترميز النص المقدم إلى صيغة يمكن للنموذج التوليدي استخدامها. وهو ضروري لتحويل المعلومات النصية إلى صيغة تكييف متوافقة. | `CLIP` |
| `نموذج_صندوق_النص_GLIGEN` | يمثل تهيئة نموذج GLIGEN المحددة المستخدمة لتوليد مربع النص. وهو ضروري لضمان توليد مربع النص وفقاً للمواصفات المطلوبة. | `GLIGEN` |
| `النص` | المحتوى النصي المراد ترميزه ودمجه في التكييف. يوفر المعلومات الدلالية التي توجه النموذج التوليدي. | `STRING` |
| `العرض` | عرض مربع النص بالبكسل. يحدد البُعد المكاني لمربع النص داخل الصورة المولدة. | `INT` |
| `الارتفاع` | ارتفاع مربع النص بالبكسل. على غرار العرض، يحدد البُعد المكاني لمربع النص داخل الصورة المولدة. | `INT` |
| `س_محور` | الإحداثي السيني للزاوية العلوية اليسرى لمربع النص داخل الصورة المولدة. يحدد موضع مربع النص أفقياً. | `INT` |
| `ص_محور` | الإحداثي الصادي للزاوية العلوية اليسرى لمربع النص داخل الصورة المولدة. يحدد موضع مربع النص عمودياً. | `INT` |

## المخرجات

| المعامل | الوصف | نوع Comfy |
| --- | --- | --- |
| `conditioning` | مخرجات التكييف المُثراة، والتي تتضمن بيانات التكييف الأصلية بالإضافة إلى معاملات مربع النص المضافة حديثاً والمعلومات النصية المشفرة. يُستخدم لتوجيه النموذج التوليدي لإنتاج مخرجات واعية بالسياق. | `CONDITIONING` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/GLIGENTextBoxApply/ar.md)
