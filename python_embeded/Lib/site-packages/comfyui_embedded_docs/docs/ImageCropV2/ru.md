# Обрезка изображения

Узел Image Crop извлекает прямоугольную область из входного изображения. Вы определяете область, которую нужно сохранить, указав координаты её верхнего левого угла, а также ширину и высоту. Узел возвращает вырезанный фрагмент исходного изображения.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `изображение` | Входное изображение, из которого будет производиться обрезка. | IMAGE | Да | Н/Д |
| `область_обрезки` | Определяет прямоугольную область для извлечения из изображения. Задаётся параметрами `x` (горизонтальное начало), `y` (вертикальное начало), `width` (ширина) и `height` (высота). Если заданная область выходит за границы изображения, она будет автоматически скорректирована, чтобы поместиться в пределах размеров изображения. | BOUNDINGBOX | Да | Н/Д |

**Примечание об ограничениях области:** Область обрезки автоматически ограничивается рамками входного изображения. Если указанная координата `x` или `y` превышает ширину или высоту изображения, она будет установлена на максимально допустимую позицию. Результирующие ширина и высота обрезки будут скорректированы таким образом, чтобы область не выходила за края изображения.

## Выходные параметры

| Имя выходного параметра | Описание | Тип данных |
| --- | --- | --- |
| `изображение` | Вырезанный фрагмент исходного входного изображения. | IMAGE |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ImageCropV2/ru.md)

---
**Source fingerprint (SHA-256):** `9d3543aa8396ae2ab0353accc3c89ae6be6495f6fdcefbb5439fa865a5d3059f`
