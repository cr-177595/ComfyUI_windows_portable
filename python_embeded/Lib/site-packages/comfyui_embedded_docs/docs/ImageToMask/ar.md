# تحويل الصورة إلى قناع

عقدة تحويل الصورة إلى قناع (ImageToMask) مصممة لتحويل صورة إلى قناع بناءً على قناة لونية محددة. تتيح هذه العقدة استخراج طبقات القناع المقابلة للقنوات الحمراء أو الخضراء أو الزرقاء أو قناة الشفافية (ألفا) للصورة، مما يسهل العمليات التي تتطلب معالجة أو إخفاءًا يعتمد على قناة محددة.

## المدخلات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `الصورة` | يمثل معامل `الصورة` الصورة المدخلة التي سيتم توليد قناع منها بناءً على القناة اللونية المحددة. يلعب دورًا حاسمًا في تحديد محتوى وخصائص القناع الناتج. | `IMAGE` |
| `القناة` | يحدد معامل `القناة` القناة اللونية (حمراء، خضراء، زرقاء، أو ألفا) من الصورة المدخلة التي سيتم استخدامها لتوليد القناع. يؤثر هذا الاختيار بشكل مباشر على مظهر القناع وأجزاء الصورة التي يتم إبرازها أو إخفاؤها. | COMBO[STRING] |

## المخرجات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `mask` | المخرجات `mask` هي تمثيل ثنائي (أبيض وأسود) أو بتدرج رمادي للقناة اللونية المحددة من الصورة المدخلة، وهي مفيدة لعمليات معالجة الصور أو الإخفاء الإضافية. | `MASK` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ImageToMask/ar.md)
