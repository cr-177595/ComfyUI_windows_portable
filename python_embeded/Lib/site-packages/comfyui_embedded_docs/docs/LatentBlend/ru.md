# Смешивание латентов

Вот перевод документации на русский язык:

Узел LatentBlend объединяет два латентных сэмпла путем их смешивания с использованием заданного коэффициента смешивания. Он принимает два латентных входа и создает новый выход, где первый сэмпл взвешивается по коэффициенту смешивания, а второй — по обратному значению. Если входные сэмплы имеют разную форму, второй сэмпл автоматически изменяет размер, чтобы соответствовать размерам первого сэмпла.

## Входы

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `образцы1` | Первый латентный сэмпл для смешивания | LATENT | Да | - |
| `образцы2` | Второй латентный сэмпл для смешивания | LATENT | Да | - |
| `фактор_смешивания` | Управляет соотношением смешивания между двумя сэмплами (по умолчанию: 0.5) | FLOAT | Да | от 0 до 1 |

**Примечание:** Если `samples1` и `samples2` имеют разную форму, `samples2` будет автоматически изменен в размере, чтобы соответствовать размерам `samples1`, с использованием бикубической интерполяции и центрированного кадрирования.

## Выходы

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `latent` | Смешанный латентный сэмпл, объединяющий оба входных сэмпла | LATENT |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LatentBlend/ru.md)

---
**Source fingerprint (SHA-256):** `a19808c5b606a8c05f2685fcd78d9f08c1ba51613a4029b36cf0ce5305618c2f`
