# TextEncodeQwenImageEdit

Вот перевод документации на русский язык:

Узел TextEncodeQwenImageEdit обрабатывает текстовые подсказки и опциональные изображения для генерации данных кондиционирования (conditioning) при создании или редактировании изображений. Он использует модель CLIP для токенизации входных данных и может опционально кодировать эталонные изображения с помощью VAE для создания эталонных латентов (reference latents). При наличии изображения узел автоматически изменяет его размер для обеспечения единообразных размеров обработки.

## Входные параметры

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `clip` | Модель CLIP, используемая для токенизации текста и изображений | CLIP | Да | - |
| `промпт` | Текстовая подсказка для генерации кондиционирования, поддерживает многострочный ввод и динамические подсказки | STRING | Да | - |
| `vae` | Опциональная модель VAE для кодирования эталонных изображений в латенты | VAE | Нет | - |
| `изображение` | Опциональное входное изображение для эталона или редактирования | IMAGE | Нет | - |

**Примечание:** Если указаны и `image`, и `vae`, узел кодирует изображение в эталонные латенты и присоединяет их к выходным данным кондиционирования. Изображение автоматически изменяется в размере для поддержания единообразного масштаба обработки примерно 1024x1024 пикселя.

## Выходные параметры

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `CONDITIONING` | Данные кондиционирования, содержащие текстовые токены и опциональные эталонные латенты для генерации изображения | CONDITIONING |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/TextEncodeQwenImageEdit/ru.md)

---
**Source fingerprint (SHA-256):** `143af2c93aa56ace3594ecb257cac9dbaef2666665f3fb6dfd7a987cd2ea326f`
