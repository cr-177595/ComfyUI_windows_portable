# Grok 影片延伸

## 概述

Grok 影片延伸節點使用 AI 模型來為現有影片建立無縫的延續內容。您提供一段簡短影片以及描述接下來應發生什麼事的文字提示，該節點便會生成一段接續原始影片的新影片片段。

## 輸入

| 參數 | 說明 | 資料類型 | 必要 | 範圍 |
| --- | --- | --- | --- | --- |
| `提示詞` | 描述影片接下來應發生什麼事的文字說明。 | STRING | 是 | 不適用 |
| `影片` | 要延伸的來源影片。MP4 格式，長度為 2 至 15 秒。 | VIDEO | 是 | 不適用 |
| `模型` | 用於影片延伸的模型。選取後會顯示一個巢狀的 `duration` 參數。 | COMBO | 是 | `"grok-imagine-video"` |
| `種子` | 用於決定節點是否應重新執行的種子；無論種子為何，實際結果皆為非確定性（預設值：0）。 | INT | 否 | 0 至 2147483647 |

**參數限制：**
*   `video` 輸入必須是長度介於 2 到 15 秒之間的 MP4 檔案，且檔案大小不得超過 50MB。
*   `prompt` 必須包含至少一個字元（空白字元會被去除）。
*   `model` 參數是一個動態下拉選單。選取「grok-imagine-video」選項後，會顯示一個巢狀的 `duration` 參數，用於控制延伸片段的長度（以秒為單位，預設值：8，範圍：2 至 10）。

## 輸出

| 輸出名稱 | 說明 | 資料類型 |
| --- | --- | --- |
| `output` | 新生成的影片延伸片段。 | VIDEO |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/GrokVideoExtendNode/zh-TW.md)

---
**Source fingerprint (SHA-256):** `a33383be0eb6857538a75e1b901ee58df0153dfeaf95a7ee19933d651b745b5f`
