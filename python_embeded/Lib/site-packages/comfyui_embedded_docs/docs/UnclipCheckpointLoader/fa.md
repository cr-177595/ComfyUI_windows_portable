# unCLIPCheckpointLoader

این گره مدل‌های موجود در پوشه `ComfyUI/models/checkpoints` را شناسایی می‌کند و همچنین مدل‌های مسیرهای اضافی پیکربندی‌شده در فایل extra_model_paths.yaml را می‌خواند. گاهی اوقات ممکن است نیاز به **بازنشانی (رفرش) رابط کاربری ComfyUI** داشته باشید تا بتواند فایل‌های مدل را از پوشه مربوطه بخواند.

گره unCLIPCheckpointLoader برای بارگذاری نقاط بازبینی (checkpoints) طراحی شده است که به‌طور خاص برای مدل‌های unCLIP تنظیم شده‌اند. این گره فرایند بازیابی و مقداردهی اولیه مدل‌ها، ماژول‌های CLIP vision و VAE را از یک نقطه بازبینی مشخص تسهیل می‌کند و فرایند راه‌اندازی را برای عملیات یا تحلیل‌های بعدی ساده‌تر می‌سازد.

## ورودی‌ها

| فیلد | توضیحات | نوع داده Comfy |
| --- | --- | --- |
| `نام Checkpoint` | نام نقطه بازبینی که باید بارگذاری شود را مشخص می‌کند. فایل نقطه بازبینی صحیح را از یک دایرکتوری از پیش تعریف‌شده شناسایی و بازیابی کرده و نحوه مقداردهی اولیه مدل‌ها و پیکربندی‌ها را تعیین می‌کند. | `COMBO[STRING]` |

## خروجی‌ها

| فیلد | توضیحات | نوع داده Comfy | نوع داده پایتون |
| --- | --- | --- | --- |
| `model` | نشان‌دهنده مدل اصلی بارگذاری‌شده از نقطه بازبینی است. | `MODEL` | `torch.nn.Module` |
| `clip` | نشان‌دهنده ماژول CLIP بارگذاری‌شده از نقطه بازبینی (در صورت وجود) است. | `CLIP` | `torch.nn.Module` |
| `vae` | نشان‌دهنده ماژول VAE بارگذاری‌شده از نقطه بازبینی (در صورت وجود) است. | `VAE` | `torch.nn.Module` |
| `clip_vision` | نشان‌دهنده ماژول vision CLIP بارگذاری‌شده از نقطه بازبینی (در صورت وجود) است. | `CLIP_VISION` | `torch.nn.Module` |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/unCLIPCheckpointLoader/fa.md)
