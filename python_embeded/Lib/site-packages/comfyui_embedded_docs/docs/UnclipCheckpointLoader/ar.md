# محمل نقطة فحص unCLIP

سيكتشف هذا العقد النماذج الموجودة في مجلد `ComfyUI/models/checkpoints`، كما سيقرأ النماذج من المسارات الإضافية المُهيأة في ملف extra_model_paths.yaml. قد تحتاج أحيانًا إلى **تحديث واجهة ComfyUI** للسماح لها بقراءة ملفات النموذج من المجلد المقابل.

تم تصميم عقدة unCLIPCheckpointLoader لتحميل نقاط التفتيش المخصصة لنماذج unCLIP تحديدًا. وهي تُسهّل استرجاع وتهيئة النماذج ووحدات CLIP البصرية ووحدات VAE من نقطة تفتيش محددة، مما يُبسّط عملية الإعداد للعمليات أو التحليلات اللاحقة.

## المدخلات

| الحقل | الوصف | نوع Comfy |
| --- | --- | --- |
| `اسم نقطة الفحص` | يُحدد اسم نقطة التفتيش المراد تحميلها، ويقوم بتعريف واسترجاع ملف نقطة التفتيش الصحيح من دليل مُحدد مسبقًا، مما يُحدد تهيئة النماذج والتكوينات. | `COMBO[STRING]` |

## المخرجات

| الحقل | الوصف | نوع Comfy | نوع Python |
| --- | --- | --- | --- |
| `model` | يُمثل النموذج الأساسي المُحمّل من نقطة التفتيش. | `MODEL` | `torch.nn.Module` |
| `clip` | يُمثل وحدة CLIP المُحمّلة من نقطة التفتيش، إن وُجدت. | `CLIP` | `torch.nn.Module` |
| `vae` | يُمثل وحدة VAE المُحمّلة من نقطة التفتيش، إن وُجدت. | `VAE` | `torch.nn.Module` |
| `clip_vision` | يُمثل وحدة CLIP البصرية المُحمّلة من نقطة التفتيش، إن وُجدت. | `CLIP_VISION` | `torch.nn.Module` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/unCLIPCheckpointLoader/ar.md)
