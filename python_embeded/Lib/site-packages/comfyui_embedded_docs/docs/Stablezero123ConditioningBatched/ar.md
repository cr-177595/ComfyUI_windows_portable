# Stablezero123ConditioningBatched

### نظرة عامة

تم تصميم هذه العقدة لمعالجة معلومات التكييف (conditioning) بطريقة مجمعة (batched) ومخصصة خصيصًا لنموذج StableZero123. تركز العقدة على التعامل بكفاءة مع مجموعات متعددة من بيانات التكييف في وقت واحد، مما يحسن سير العمل في السيناريوهات التي تكون فيها المعالجة المجمعة أمرًا بالغ الأهمية.

### المدخلات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `clip_vision` | تضمينات CLIP البصرية التي توفر سياقًا بصريًا لعملية التكييف. | `CLIP_VISION` |
| `init_image` | الصورة الأولية التي سيتم التكييف عليها، وتعمل كنقطة انطلاق لعملية التوليد. | `IMAGE` |
| `vae` | المشفر التلقائي المتغير (VAE) المستخدم لتشفير وفك تشفير الصور في عملية التكييف. | `VAE` |
| `width` | عرض الصورة الناتجة. | `INT` |
| `height` | ارتفاع الصورة الناتجة. | `INT` |
| `batch_size` | عدد مجموعات التكييف التي سيتم معالجتها في دفعة واحدة. | `INT` |
| `elevation` | زاوية الارتفاع (elevation) لتكييف النموذج ثلاثي الأبعاد، مما يؤثر على منظور الصورة المولدة. | `FLOAT` |
| `azimuth` | زاوية السمت (azimuth) لتكييف النموذج ثلاثي الأبعاد، مما يؤثر على اتجاه الصورة المولدة. | `FLOAT` |
| `elevation_batch_increment` | التغيير التدريجي في زاوية الارتفاع عبر الدفعة، مما يسمح بمنظورات متنوعة. | `FLOAT` |
| `azimuth_batch_increment` | التغيير التدريجي في زاوية السمت عبر الدفعة، مما يسمح باتجاهات متنوعة. | `FLOAT` |

### المخرجات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `positive` | مخرج التكييف الإيجابي، المصمم لتعزيز ميزات أو جوانب معينة في المحتوى المولد. | `CONDITIONING` |
| `negative` | مخرج التكييف السلبي، المصمم لتقليل ميزات أو جوانب معينة في المحتوى المولد. | `CONDITIONING` |
| `latent` | التمثيل الكامن (latent representation) المستمد من عملية التكييف، الجاهز لمزيد من المعالجة أو خطوات التوليد. | `LATENT` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/Stablezero123ConditioningBatched/ar.md)
