# جدول SD Turbo

مُجدول SDTurboScheduler مُصمم لتوليد سلسلة من قيم سيغما (sigma) لأخذ عينات الصور، مع ضبط السلسلة بناءً على مستوى إزالة التشويش (denoise) وعدد الخطوات المُحددة. يستفيد من قدرات أخذ العينات لنموذج معين لإنتاج قيم سيغما هذه، والتي تُعد ضرورية للتحكم في عملية إزالة التشويش أثناء توليد الصور.

## المدخلات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `النموذج` | يُحدد معامل النموذج النموذج التوليدي الذي سيتم استخدامه لتوليد قيم سيغما. وهو ضروري لتحديد سلوك أخذ العينات وقدرات المُجدول. | `MODEL` |
| `الخطوات` | يُحدد معامل الخطوات طول سلسلة سيغما المراد توليدها، مما يؤثر بشكل مباشر على دقة عملية إزالة التشويش. | `INT` |
| `إزالة التشويش` | يُعدّل معامل إزالة التشويش نقطة البداية لسلسلة سيغما، مما يتيح تحكمًا أدق في مستوى إزالة التشويش المُطبق أثناء توليد الصور. | `FLOAT` |

## المخرجات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `sigmas` | سلسلة من قيم سيغما تم توليدها بناءً على النموذج والخطوات ومستوى إزالة التشويش المُحددة. هذه القيم ضرورية للتحكم في عملية إزالة التشويش أثناء توليد الصور. | `SIGMAS` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/SDTurboScheduler/ar.md)
