# FluxProCannyNode

### نمای کلی

تصویری را با استفاده از یک تصویر کنترلی (canny) تولید کنید. این گره یک تصویر کنترلی دریافت کرده و بر اساس prompt ارائه‌شده، تصویر جدیدی تولید می‌کند که از ساختار لبه‌های شناسایی‌شده در تصویر کنترلی پیروی می‌کند.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | ضروری | محدوده |
| --- | --- | --- | --- | --- |
| `control_image` | تصویر ورودی که برای کنترل تشخیص لبه Canny استفاده می‌شود | IMAGE | بله | - |
| `prompt` | prompt برای تولید تصویر (پیش‌فرض: رشته خالی) | STRING | خیر | - |
| `prompt_upsampling` | آیا بر روی prompt افزایش نمونه (upsampling) انجام شود یا خیر. در صورت فعال بودن، به‌طور خودکار prompt را برای تولید خلاقانه‌تر تغییر می‌دهد، اما نتایج غیرقطعی هستند (همان seed نتیجه دقیقاً یکسانی تولید نمی‌کند). (پیش‌فرض: False) | BOOLEAN | خیر | - |
| `canny_low_threshold` | آستانه پایین برای تشخیص لبه Canny؛ در صورت True بودن `skip_preprocessing` نادیده گرفته می‌شود (پیش‌فرض: 0.1) | FLOAT | خیر | 0.01 - 0.99 |
| `canny_high_threshold` | آستانه بالا برای تشخیص لبه Canny؛ در صورت True بودن `skip_preprocessing` نادیده گرفته می‌شود (پیش‌فرض: 0.4) | FLOAT | خیر | 0.01 - 0.99 |
| `skip_preprocessing` | آیا پیش‌پردازش نادیده گرفته شود یا خیر؛ اگر `control_image` از قبل به تصویر Canny تبدیل شده است، True و اگر تصویر خام است، False تنظیم کنید. (پیش‌فرض: False) | BOOLEAN | خیر | - |
| `guidance` | قدرت هدایت برای فرآیند تولید تصویر (پیش‌فرض: 30) | FLOAT | خیر | 1 - 100 |
| `steps` | تعداد مراحل برای فرآیند تولید تصویر (پیش‌فرض: 50) | INT | خیر | 15 - 50 |
| `seed` | seed تصادفی مورد استفاده برای ایجاد نویز. (پیش‌فرض: 0) | INT | خیر | 0 - 18446744073709551615 |

**نکته:** هنگامی که `skip_preprocessing` روی True تنظیم شده است، پارامترهای `canny_low_threshold` و `canny_high_threshold` نادیده گرفته می‌شوند، زیرا فرض بر این است که تصویر کنترلی از قبل به عنوان یک تصویر لبه Canny پردازش شده است. سپس `control_image` مستقیماً به عنوان تصویر پیش‌پردازش‌شده استفاده می‌شود.

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
| --- | --- | --- |
| `output_image` | تصویر تولیدشده بر اساس تصویر کنترلی و prompt | IMAGE |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/FluxProCannyNode/fa.md)

---
**Source fingerprint (SHA-256):** `dedf55a2b2c183519d7f5be0d9a96abbe40716a247f574fc0d50f10f715949a7`
