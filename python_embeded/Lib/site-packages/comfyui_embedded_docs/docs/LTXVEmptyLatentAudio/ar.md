# LTXV Empty Latent Audio

أنت خبير في الترجمة التقنية متخصص في توثيق عُقد ComfyUI من الإنجليزية إلى العربية.

## قواعد الترجمة

1. **المحتوى الذي يجب عدم ترجمته:**
   - أسماء المعاملات بين علامات الاقتباس الخلفية: `image`, `seed`, `model`
   - أنواع البيانات بالأحرف الكبيرة: IMAGE, STRING, INT, FLOAT, MODEL, CONDITIONING, إلخ
   - القيم في عمود Range: الأرقام، "auto"، أسماء الخيارات
   - الكود، مسارات الملفات

2. **المحتوى الذي يجب ترجمته:**
   - عناوين الأقسام: ## نظرة عامة, ## المدخلات, ## المخرجات
   - جميع النصوص الوصفية والتوضيحية
   - أوصاف المعاملات

3. **جودة الترجمة:**
   - استخدام اللغة العربية الفصحى المعاصرة
   - الحفاظ على نبرة احترافية ولكن سهلة الفهم
   - ضمان الدقة التقنية
   - استخدام المصطلحات التقنية العربية القياسية

4. **التنسيق:**
   - الحفاظ على جميع تنسيقات Markdown
   - الحفاظ على بنية الجداول
   - عدم إضافة أي ملاحظة أو رابط في بداية الوثيقة (سيتم إضافتها تلقائيًا)

الرجاء ترجمة الوثيقة التالية إلى العربية، دون تضمين الملاحظة الأولية للوثيقة:

> تم إنشاء هذه الوثائق بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [عدّل على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LTXVEmptyLatentAudio/en.md)

تنشئ عقدة LTXV للكمون الصوتي الفارغ مجموعة من موترات الكمون الصوتي الفارغة (المملوءة بالأصفار). تستخدم التهيئة من نموذج VAE الصوتي المقدم لتحديد الأبعاد الصحيحة للفضاء الكامن، مثل عدد القنوات ونطاقات التردد. يعمل هذا الكمون الفارغ كنقطة انطلاق لسير عمل توليد الصوت أو معالجته داخل ComfyUI.

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
| --- | --- | --- | --- | --- |
| `frames_number` | عدد الإطارات. القيمة الافتراضية هي 97. | INT | نعم | 1 إلى 1000 |
| `frame_rate` | عدد الإطارات في الثانية. القيمة الافتراضية هي 25. | INT | نعم | 1 إلى 1000 |
| `batch_size` | عدد عينات الكمون الصوتي في الدفعة. القيمة الافتراضية هي 1. | INT | نعم | 1 إلى 4096 |
| `audio_vae` | نموذج VAE الصوتي للحصول على التهيئة منه. هذا المعامل إلزامي. | VAE | نعم | غير متاح |

**ملاحظة:** مدخل `audio_vae` إلزامي. ستقوم العقدة بإظهار خطأ إذا لم يتم توفيره.

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `Latent` | موتر كمون صوتي فارغ بالهيكل (batch_size, z_channels, num_audio_latents, audio_freq) مهيأ ليتوافق مع VAE الصوتي المُدخل. يتضمن المخرج أيضًا حقل `type` مضبوطًا على "audio". | LATENT |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LTXVEmptyLatentAudio/ar.md)

---
**Source fingerprint (SHA-256):** `1a8bfea98f14de014069016652b39542cfd9290cae2d870ab4e381e46aa1e08f`
