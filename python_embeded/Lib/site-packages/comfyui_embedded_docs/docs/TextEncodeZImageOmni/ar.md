# TextEncodeZImageOmni

عقدة TextEncodeZImageOmni هي عقدة تكييف متقدمة تقوم بتشفير موجه نصي مع صور مرجعية اختيارية إلى تنسيق تكييف مناسب لنماذج توليد الصور. يمكنها معالجة ما يصل إلى ثلاث صور، مع تشفيرها اختياريًا باستخدام مشفر رؤية و/أو VAE لإنتاج مراجع كامنة، وتدمج هذه المراجع البصرية مع الموجه النصي باستخدام هيكل قالب محدد.

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
| --- | --- | --- | --- | --- |
| `clip` | نموذج CLIP المستخدم لتقطيع وتشفير الموجه النصي. | CLIP | نعم |  |
| `مُرمّز الصورة` | نموذج مشفر رؤية اختياري. إذا تم توفيره، سيتم استخدامه لتشفير الصور المدخلة، وستتم إضافة التضمينات الناتجة إلى التكييف. | CLIPVision | لا |  |
| `التوجيه` | الموجه النصي المراد تشفيره. يدعم هذا الحقل الإدخال متعدد الأسطر والموجهات الديناميكية. | STRING | نعم |  |
| `تغيير حجم الصور تلقائياً` | عند التمكين (الافتراضي: صحيح)، سيتم تغيير حجم الصور المدخلة تلقائيًا بناءً على مساحة البكسل الخاصة بها قبل تمريرها إلى VAE للتشفير. | BOOLEAN | لا |  |
| `vae` | نموذج VAE اختياري. إذا تم توفيره، سيتم استخدامه لتشفير الصور المدخلة إلى تمثيلات كامنة، والتي تضاف إلى التكييف كمراجع كامنة. | VAE | لا |  |
| `الصورة ١` | الصورة المرجعية الاختيارية الأولى. | IMAGE | لا |  |
| `الصورة ٢` | الصورة المرجعية الاختيارية الثانية. | IMAGE | لا |  |
| `الصورة ٣` | الصورة المرجعية الاختيارية الثالثة. | IMAGE | لا |  |

**ملاحظة:** يمكن للعقدة استقبال حد أقصى ثلاث صور (`image1`، `image2`، `image3`). يتم استخدام مدخلات `image_encoder` و `vae` فقط إذا تم توفير صورة واحدة على الأقل. عندما تكون `auto_resize_images` صحيحة ويكون `vae` متصلاً، يتم تغيير حجم الصور بحيث تكون مساحة البكسل الإجمالية قريبة من 1024×1024 قبل التشفير.

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `CONDITIONING` | مخرج التكييف النهائي، الذي يحتوي على الموجه النصي المشفر وقد يتضمن تضمينات الصور المشفرة و/أو المراجع الكامنة إذا تم توفير الصور. | CONDITIONING |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/TextEncodeZImageOmni/ar.md)

---
**Source fingerprint (SHA-256):** `daa4205acdf72503180eeedb4142708d239d4ff0f689012a298264ae2d8ea949`
