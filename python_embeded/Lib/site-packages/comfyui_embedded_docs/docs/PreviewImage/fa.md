# پیش‌نمایش تصویر

گره PreviewImage برای ایجاد تصاویر پیش‌نمایش موقت طراحی شده است. این گره به‌طور خودکار یک نام فایل موقت یکتا برای هر تصویر تولید می‌کند، تصویر را تا سطح مشخصی فشرده می‌سازد و آن را در یک دایرکتوری موقت ذخیره می‌کند. این قابلیت به‌ویژه برای تولید پیش‌نمایش تصاویر در حین پردازش، بدون تأثیر بر فایل‌های اصلی، مفید است.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده |
| --- | --- | --- |
| `تصاویر` | ورودی `تصاویر` تصاویری را مشخص می‌کند که باید پردازش شده و به‌عنوان تصاویر پیش‌نمایش موقت ذخیره شوند. این ورودی اصلی گره است و تعیین می‌کند کدام تصاویر تحت فرآیند تولید پیش‌نمایش قرار گیرند. | `IMAGE` |

## خروجی‌ها

این گره نوع خروجی ندارد.

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/PreviewImage/fa.md)
