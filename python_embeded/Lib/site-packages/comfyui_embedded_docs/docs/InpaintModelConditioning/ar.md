# تكوين نموذج التلوين

عُقدة **InpaintModelConditioning** مصممة لتسهيل عملية التكييف (conditioning) لنماذج إعادة التلوين (inpainting)، مما يتيح دمج ومعالجة مدخلات التكييف المختلفة لتخصيص مخرجات إعادة التلوين. تشمل هذه العقدة مجموعة واسعة من الوظائف، بدءًا من تحميل نقاط تفتيش نموذجية محددة وتطبيق نماذج الأنماط أو التحكم، وصولًا إلى ترميز ودمج عناصر التكييف، مما يجعلها أداة شاملة لتخصيص مهام إعادة التلوين.

## المدخلات

| المعامل | الوصف | نوع Comfy |
| --- | --- | --- |
| `إيجابي` | يمثل معلومات أو معاملات التكييف الإيجابية التي سيتم تطبيقها على نموذج إعادة التلوين. يُعد هذا المدخل ضروريًا لتحديد السياق أو القيود التي يجب أن تتم بموجبها عملية إعادة التلوين، مما يؤثر بشكل كبير على المخرجات النهائية. | `CONDITIONING` |
| `سلبي` | يمثل معلومات أو معاملات التكييف السلبية التي سيتم تطبيقها على نموذج إعادة التلوين. يُعد هذا المدخل ضروريًا لتحديد الشروط أو السياقات التي يجب تجنبها أثناء عملية إعادة التلوين، مما يؤثر بالتالي على المخرجات النهائية. | `CONDITIONING` |
| `vae` | يحدد نموذج VAE الذي سيتم استخدامه في عملية التكييف. يُعد هذا المدخل ضروريًا لتحديد البنية والمعاملات المحددة لنموذج VAE الذي سيتم استخدامه. | `VAE` |
| `بكسلات` | يمثل بيانات البكسل للصورة المراد إعادة تلوينها. يُعد هذا المدخل ضروريًا لتوفير السياق البصري اللازم لمهمة إعادة التلوين. | `IMAGE` |
| `قناع` | يحدد القناع (mask) الذي سيتم تطبيقه على الصورة، للإشارة إلى المناطق المراد إعادة تلوينها. يُعد هذا المدخل ضروريًا لتحديد المناطق المحددة داخل الصورة التي تتطلب إعادة تلوين. | `MASK` |

## المخرجات

| المعامل | الوصف | نوع البيانات |
| --- | --- | --- |
| `سلبي` | معلومات التكييف الإيجابية المعدلة بعد المعالجة، الجاهزة للتطبيق على نموذج إعادة التلوين. يُعد هذا المخرج ضروريًا لتوجيه عملية إعادة التلوين وفقًا للشروط الإيجابية المحددة. | `CONDITIONING` |
| `المتغير الكامن` | معلومات التكييف السلبية المعدلة بعد المعالجة، الجاهزة للتطبيق على نموذج إعادة التلوين. يُعد هذا المخرج ضروريًا لتوجيه عملية إعادة التلوين وفقًا للشروط السلبية المحددة. | `CONDITIONING` |
| `latent` | التمثيل الكامن (latent representation) المستمد من عملية التكييف. يُعد هذا المخرج ضروريًا لفهم السمات والخصائص الأساسية للصورة التي يتم إعادة تلوينها. | `LATENT` |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/InpaintModelConditioning/ar.md)
