# تحسين الصورة بواسطة Topaz

عقدة تحسين الصور من Topaz توفر تحسينًا احترافيًا للصور ورفع دقتها. تقوم بمعالجة صورة إدخال واحدة باستخدام نموذج ذكاء اصطناعي قائم على السحابة لتحسين الجودة والتفاصيل والدقة. تمنح العقدة تحكمًا دقيقًا في عملية التحسين، بما في ذلك خيارات التوجيه الإبداعي والتركيز على الموضوع والحفاظ على الوجوه.

## المدخلات

| المعامل | الوصف | نوع البيانات | إلزامي | النطاق |
| --- | --- | --- | --- | --- |
| `النموذج` | نموذج الذكاء الاصطناعي المستخدم لتحسين الصورة. | COMBO | نعم | `"Reimagine"` |
| `الصورة` | صورة الإدخال المراد تحسينها. يتم دعم صورة واحدة فقط. | IMAGE | نعم | - |
| `الموجه` | نص وصفي اختياري لتوجيه تحسين الصورة بشكل إبداعي (الافتراضي: فارغ). | STRING | لا | - |
| `اكتشاف الموضوع` | يتحكم في أي جزء من الصورة يركز عليه التحسين (الافتراضي: "All"). | COMBO | لا | `"All"`<br>`"Foreground"`<br>`"Background"` |
| `تحسين الوجه` | تفعيل لتحسين الوجوه إذا كانت موجودة في الصورة (الافتراضي: True). | BOOLEAN | لا | - |
| `إبداعية تحسين الوجه` | يحدد مستوى الإبداع لتحسين الوجوه (الافتراضي: 0.0). | FLOAT | لا | 0.0 - 1.0 |
| `قوة تحسين الوجه` | يتحكم في مدى وضوح الوجوه المحسنة مقارنة بالخلفية (الافتراضي: 1.0). | FLOAT | لا | 0.0 - 1.0 |
| `قص لملء` | افتراضيًا، يتم إضافة أشرطة سوداء جانبية عند اختلاف نسبة أبعاد الإخراج. قم بالتفعيل لقص الصورة لملء أبعاد الإخراج بدلاً من ذلك (الافتراضي: False). | BOOLEAN | لا | - |
| `عرض الإخراج` | العرض المطلوب لصورة الإخراج. القيمة 0 تعني الحساب التلقائي، عادةً بناءً على الحجم الأصلي أو `ارتفاع الإخراج` إذا تم تحديده (الافتراضي: 0). | INT | لا | 0 - 32000 |
| `ارتفاع الإخراج` | الارتفاع المطلوب لصورة الإخراج. القيمة 0 تعني الحساب التلقائي، عادةً بناءً على الحجم الأصلي أو `عرض الإخراج` إذا تم تحديده (الافتراضي: 0). | INT | لا | 0 - 32000 |
| `الإبداع` | يتحكم في المستوى العام للإبداع في التحسين (الافتراضي: 3). | INT | لا | 1 - 9 |
| `حفظ الوجه` | الحفاظ على هوية الوجوه للأشخاص في الصورة (الافتراضي: True). | BOOLEAN | لا | - |
| `حفظ الألوان` | الحفاظ على الألوان الأصلية لصورة الإدخال (الافتراضي: True). | BOOLEAN | لا | - |

**ملاحظة:** يمكن لهذه العقدة معالجة صورة إدخال واحدة فقط. سيؤدي توفير مجموعة من عدة صور إلى حدوث خطأ.

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
| --- | --- | --- |
| `الصورة` | صورة الإخراج المحسنة. | IMAGE |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/TopazImageEnhance/ar.md)

---
**Source fingerprint (SHA-256):** `69f2c929f2cd11f13557e064e30a4514e3862e127a2bdb3a3f40ec92023f255d`
