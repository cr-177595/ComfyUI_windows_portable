# Кодирование текста CLIP SD3

Вот перевод документации на русский язык:

Узел CLIPTextEncodeSD3 обрабатывает текстовые вводы для моделей Stable Diffusion 3 путем кодирования нескольких текстовых подсказок с использованием различных моделей CLIP. Он обрабатывает три отдельных текстовых ввода (`clip_g`, `clip_l` и `t5xxl`) и предоставляет опции для управления заполнением пустого текста. Узел обеспечивает правильное выравнивание токенов между различными текстовыми вводами и возвращает данные conditioning, подходящие для конвейеров генерации SD3.

## Входы

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
| --- | --- | --- | --- | --- |
| `clip` | Модель CLIP, используемая для кодирования текста | CLIP | Да | - |
| `clip_l` | Текстовый ввод для локальной модели CLIP. Поддерживает многострочный текст и динамические подсказки. | STRING | Да | - |
| `clip_g` | Текстовый ввод для глобальной модели CLIP. Поддерживает многострочный текст и динамические подсказки. | STRING | Да | - |
| `t5xxl` | Текстовый ввод для модели T5-XXL. Поддерживает многострочный текст и динамические подсказки. | STRING | Да | - |
| `пустое_заполнение` | Управляет обработкой пустых текстовых вводов. При значении "none" пустые текстовые вводы для `clip_g`, `clip_l` или `t5xxl` приведут к пустым спискам токенов вместо заполнения. Это расширенный параметр (по умолчанию: "none"). | COMBO | Да | `"none"`<br>`"empty_prompt"` |

**Ограничения параметров:**

- Когда `empty_padding` установлен в "none", пустые текстовые вводы для `clip_g`, `clip_l` или `t5xxl` приведут к пустым спискам токенов вместо заполнения
- Узел автоматически выравнивает длину токенов между вводами `clip_l` и `clip_g`, дополняя более короткий пустыми токенами при различии в длине
- Все текстовые вводы поддерживают динамические подсказки и многострочный ввод текста

## Выходы

| Имя выхода | Описание | Тип данных |
| --- | --- | --- |
| `CONDITIONING` | Закодированные данные conditioning, готовые к использованию в конвейерах генерации SD3 | CONDITIONING |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/CLIPTextEncodeSD3/ru.md)

---
**Source fingerprint (SHA-256):** `38f7538d05fe48e74f41f265550b83906b2f0c5d31f0783f6859f4df7b5cb9d3`
