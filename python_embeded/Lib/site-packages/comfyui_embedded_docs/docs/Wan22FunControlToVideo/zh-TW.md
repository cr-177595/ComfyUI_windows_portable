# Wan22FunControlToVideo

Wan22FunControlToVideo 節點使用 Wan 影片模型架構，為影片生成準備條件化（conditioning）與潛在空間（latent）表示。它處理正向與負向條件化輸入，以及可選的參考影像和控制影片，以建立影片合成所需的潛在空間表示。該節點處理空間縮放和時間維度，以生成適合影片模型的條件化資料。

## 輸入

| 參數 | 說明 | 資料類型 | 必要 | 範圍 |
| --- | --- | --- | --- | --- |
| `正面提示詞` | 用於引導影片生成的正向條件化輸入 | CONDITIONING | 是 | - |
| `負面提示詞` | 用於引導影片生成的負向條件化輸入 | CONDITIONING | 是 | - |
| `VAE` | 用於將影像編碼至潛在空間的 VAE 模型 | VAE | 是 | - |
| `寬度` | 輸出影片的寬度（像素）（預設值：832，步進值：16） | INT | 是 | 16 至 MAX_RESOLUTION |
| `高度` | 輸出影片的高度（像素）（預設值：480，步進值：16） | INT | 是 | 16 至 MAX_RESOLUTION |
| `長度` | 影片序列中的影格數（預設值：81，步進值：4） | INT | 是 | 1 至 MAX_RESOLUTION |
| `批次大小` | 要生成的影片序列數量（預設值：1） | INT | 是 | 1 至 4096 |
| `參考圖像` | 可選的參考影像，用於提供視覺引導 | IMAGE | 否 | - |
| `控制影片` | 可選的控制影片，用於引導生成過程 | IMAGE | 否 | - |

**注意：** `length` 參數以 4 個影格為一組進行處理，且節點會自動處理潛在空間的時間縮放。當提供 `ref_image` 時，它會透過參考潛在變數影響條件化。當提供 `control_video` 時，它會直接影響條件化中使用的串接潛在表示。`start_image` 參數並未在此節點的架構中作為輸入暴露，但在執行邏輯中被引用。

## 輸出

| 輸出名稱 | 說明 | 資料類型 |
| --- | --- | --- |
| `負面提示詞` | 修改後的正向條件化，包含影片專屬的潛在資料，包括串接潛在變數、遮罩及可選的參考潛在變數 | CONDITIONING |
| `潛在空間` | 修改後的負向條件化，包含影片專屬的潛在資料，包括串接潛在變數、遮罩及可選的參考潛在變數 | CONDITIONING |
| `latent` | 空的潛在張量，具有適合影片生成的維度，基於批次大小、潛在通道數以及空間/時間縮放 | LATENT |

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/Wan22FunControlToVideo/zh-TW.md)

---
**Source fingerprint (SHA-256):** `8b24058f06aa9f779371a402c41cffc95d13ad0131d23d1438067d77755c73e2`
